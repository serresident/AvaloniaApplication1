# Журнал Разработки (Development Log) — HMI Dashboard & IoT Gateway

В данном журнале фиксируются важные вехи разработки, изменения в ТЗ и согласованные архитектурные решения. Журнал ведется для предотвращения потери контекста при сбросе сессий чата.

---

## 📅 10.06.2026 (Сессия 1 & 2)

### 📌 Достижение: Реализация MDI-интерфейса и переход на MQTT
*   **Сделано:** Создан полноценный интерфейс внутренних плавающих окон (MDI) с поддержкой Drag-and-Drop, ресайза за углы и управления наложением окон (Bring-to-Front по клику).
*   **MQTT-миграция:** Отключен симулятор в `DataCoreService.cs`. Настройки по умолчанию переключены на тестовый брокер `stp10` и префикс топиков `gMqt/`.

### 📌 Режим мнемосхемы (Мнемосхема SCADA) — Инфраструктурный этап (Выполнено)
*   **Конфигурация:** В `HmiConfiguration.cs` и `WidgetConfig.cs` добавлены свойства: `CellSize` (для сетки 20x20 на мнемосхеме), `PipePoints` (точки отрисовки сегментов труб), `ActiveColor`/`InactiveColor`, `ValveType` (CutOff/Regulating) и `ShowFlanges`.
*   **ViewModels:** Созданы специализированные `PipeWidgetViewModel`, `ValveWidgetViewModel`, `TankWidgetViewModel`, `PumpWidgetViewModel` для новых виджетов.
*   **Отрисовка труб:** Написан кастомный класс `PipeControl.cs` для гибкого рендеринга сложных трубопроводов с поддержкой 3D-эффекта объема, соединений под углами 45°/90° и фланцев.
*   **Фиксация прогресса:** Создана папка `docs/` в проекте с планом (`docs/implementation_plan.md`), трекером задач (`docs/task.md`), регламентом (`docs/README.md`) и данным журналом для защиты от сброса сессий.

---

## 📅 10.06.2026 (Сессия 3 — Выполнение плана)

### 📌 Достижение: Интеграция SCADA Мнемосхемы и промышленных виджетов
*   **XAML-шаблоны виджетов:** В `DashboardView.axaml` добавлены визуальные стили для:
    *   `Pipe` (отрисовывается через `PipeControl` с поддержкой Hex-цветов).
    *   `Valve` (значок в виде треугольников, вершина к вершине, отображение названия и состояния "OPEN/CLOSED" или уставки "%").
    *   `Tank` (емкость с заполнением уровня через вертикальный `ProgressBar`).
    *   `Pump` (круглый насос с крыльчаткой).
*   **Вкладки навигации:** В `MainWindow.axaml` встроены кнопки-вкладки ("Дашборд" и "Мнемосхема") для быстрого переключения режимов с биндингом цвета активной вкладки.
*   **Динамические MDI-пульты:** Задвижки и насосы в режиме Runtime при клике открывают плавающие пульты управления (через генерацию динамического дашборда):
    *   Для отсечного клапана/насоса — тумблер START/STOP или OPEN/CLOSE.
    *   Для регулирующего клапана — поле ввода уставки `SetValue` с виртуальным нумпадом и ползунком `Slider`.
*   **Интерактивная физическая симуляция:** В `DataCoreService.cs` раскомментирован симуляционный цикл и добавлена динамическая симуляция процессов смесителя (уровень в баке B4305 растет/падает от работы насоса и открытия клапанов, температура смеси вычисляется от соотношения воды и пара, давление зависит от уровня жидкости).
*   **Дефолтная конфигурация:** В `ConfigurationService.cs` сгенерирована схема пароводосмесителя с 12 виджетами (трубы, насосы, задвижки, бак, датчики температуры/давления) на сетке 20x20. Добавлен автоимпорт мнемосхемы в старый `config.json`.
*   **Редактор виджетов:** В `WidgetEditorViewModel.cs` и `WidgetEditorWindow.axaml` добавлены визуальные настройки для новых полей (ввод точек трубопровода `PipePoints`, чекбокс `ShowFlanges`, тип клапана `ValveType`, выбор `ActiveColor`/`InactiveColor`).

---

## 📅 10.06.2026 (Сессия 4 — Регулировка сетки и предотвращение дублирования окон)

### 📌 Достижение: Улучшение UX плавающих окон и мелкая сетка 10x10
*   **Сетка 10x10 и перерасчет схемы:** Размер ячейки сетки мнемосхемы `Mimic.CellSize` уменьшен с `20` до `10` пикселей во вьюмодели и сервисе конфигурации. Координаты и размеры всех 12 виджетов мнемосхемы, а также точки сегментов трубопроводов (`PipePoints`) умножены на 2. Схема пароводосмесителя сохранила пропорции, но позиционируется в два раза точнее.
*   **Автообновление конфигов:** В `ConfigurationService.LoadConfigurationAsync` добавлено условие автоматической миграции/перезаписи старой мнемосхемы `20x20` на новую `10x10` при считывании существующего файла `config.json`.
*   **Адаптивные трубопроводы:** Класс `PipeControl.cs` переработан. Толщина трубы, диаметр угловых соединений, а также ширина и высота фланцев теперь рассчитываются динамически от размера ячейки `CellSize`. Трубы выглядят аккуратно как на сетке 10px, так и 160px.
*   **Предотвращение дублирования окон:** Метод `OpenChildWindow` в `MainViewModel.cs` теперь возвращает ссылку на открытое окно. Во вьюмоделях `ValveWidgetViewModel.cs` и `PumpWidgetViewModel.cs` реализовано отслеживание открытого окна: повторный клик по элементу закрывает открытый пульт управления, а закрытие по крестику корректно сбрасывает ссылку.

---
*Следующий шаг: Финальная верификация работы приложения с сеткой 10x10, адаптивными трубами и закрытием окон по повторному клику.*

---

## 📅 15.06.2026 (Сессия 5 — Переработка контрола клапана)

### 📌 Достижение: Единый промышленный ValveControl с поддержкой приводов и поворота
*   **Новый контрол клапана:** Создан класс `ValveControl.cs` (взамен старого `RegulatingValveControl.cs`), реализующий единую логику отрисовки задвижек/клапанов.
*   **Типы приводов (ActuatorType):** Поддержаны 4 режима отображения привода над штоком:
    *   `Solenoid` (электромагнитный/электропривод) — желтый прямоугольник с буквой `S` (для отсечных) или `P` (для регулирующих) согласно стилистике SCADA с предоставленного снимка экрана.
    *   `Diaphragm` (пневматический мембранный) — дугообразный купол с плоской тарелкой.
    *   `Manual` (ручной) — Т-образный штурвал.
    *   `None` — без привода (только корпус).
*   **Поворот на 90° (IsVertical):** Реализовано свойство `IsVertical`. При активации весь контрол (корпус, шток, фланец, привод) поворачивается на 90 градусов для идеального позиционирования на вертикальных участках трубопроводов без искажения пропорций.
*   **Инженерное меню (Design Mode):** 
    *   В модель `WidgetConfig` и вьюмодели `ValveWidgetViewModel.cs` / `WidgetEditorViewModel.cs` добавлены свойства `IsVertical` и `ActuatorType`.
    *   В форму настроек `WidgetEditorWindow.axaml` встроены ComboBox для выбора типа привода и CheckBox для включения вертикальной ориентации.
*   **Интеграция с тегами:** Цвета открытого/закрытого клапана теперь динамически берутся из настроек `ActiveColor`/`InactiveColor`. Для регулирующих клапанов цвет плавно интерполируется между ними в зависимости от положения `Feedback`.
*   **Очистка и регенерация конфигурации:** Устаревший класс `RegulatingValveControl.cs` удален. Файлы кэша `config.json` очищены для автоматического применения новых свойств при первом запуске приложения.

---
---

## 📅 16.06.2026 (Сессия 6 — Полировка UX, Контекстные Меню и Границы)

### 📌 Достижение: Внедрение инерционного скролла, исправление зависающих меню и ограничение окон
*   **Инерционное панорамирование (Spacebar + ЛКМ):**
    *   Реализована схема скролла холста при зажатом Пробеле и левой кнопке мыши с помощью tunneling-обработчиков событий мыши и клавиатуры в `DashboardView.axaml.cs`.
    *   Добавлен сглаживающий таймер `DispatcherTimer` (60 FPS) с интерполяцией `Lerp` для устранения рывков при перетаскивании.
    *   Реализовано физическое затухание скорости (инерция) при отпускании мыши «на лету» с адаптивным расчетом времени (не зависящим от фреймрейта).
    *   Курсор динамически меняется на `Hand` при зажатии Spacebar и на `SizeAll` в процессе перетаскивания.
*   **Исправление контекстных меню труб:**
    *   Устранена утечка/дублирование контекстных меню труб при многократном клике ПКМ. В `PipeControl.cs` добавлен статический трекер `_activeMenu`.
    *   При вызове нового меню (`ShowVertexContextMenu` / `ShowSegmentContextMenu`) или любом клике/драге по фону `DashboardPanel.cs`/`DashboardView.axaml.cs` старые открытые меню автоматически уничтожаются.
*   **Границы и ресайз плавающих окон MDI:**
    *   В `MainWindow.axaml.cs` (`OnTitleBarPointerMoved`) добавлено ограничение координат `Y >= 0` для перемещения окон MDI. Заголовки окон больше не могут быть перетащены под верхнюю панель меню, оставаясь всегда доступными для клика.
    *   Добавлены ограничения ресайза в `OnResizeHandlePointerMoved` — окна больше не могут быть увеличены за пределы правого или нижнего краев видимой области.
*   **Оптимизация ресайза виджетов дашборда:**
    *   В `DashboardPanel.cs` размер невидимого хит-бокса ресайза в правом нижнем углу виджетов уменьшен с `60x60` до `20x20` пикселей (точно под значок `"◢"`).
    *   Это полностью устранило ложные срабатывания изменения размеров при попытке захвата и перемещения виджетов в режиме редактирования. Курсор ресайза теперь активируется только строго над уголком.

---

## 📅 16.06.2026 (Сессия 7 — «Резиновые» соединения трубопроводов)

### 📌 Достижение: Разработка механизма Rubber-banding и геометрического прилипания
*   **Сбор подключений на старте перемещения:**
    *   В `DashboardPanel.cs` добавлен список `_connectedPipePoints`, хранящий ссылки на вьюмодели труб и индексы вершин, совпадающие с фланцами перемещаемой арматуры.
    *   В `OnPointerPressed` реализовано сканирование портов задвижек/насосов с учётом их ориентации (`IsVertical`, `Rotation`) и размеров ячеек для сопоставления с координатами труб.
*   **Динамическое обновление на дропе:**
    *   В `OnPointerReleased` при отпускании перетаскиваемого элемента новые координаты портов автоматически присваиваются связанным вершинам труб.
    *   Сеттер вьюмодели трубы (`PipePoints`) автоматически запускает процедуру нормализации `NormalizePointsAndSize()`, которая обновляет относительное смещение, размеры bounding box и сохраняет изменения в оригинальной конфигурации.
*   **Интеграция с JSON:**
    *   Обновленные координаты трубопроводов корректно сериализуются и сохраняются в `config.json` при вызове "Save Config".

---

## 📅 16.06.2026 (Сессия 8 — Архитектурный рефакторинг: Attached Properties, Direct Properties и Coercion)

### 📌 Достижение: Полная отвязка рендеринга дашборда от VM, перевод телеметрии на DirectProperty и внедрение защиты данных на уровне свойств
*   **Использование присоединенных свойств (Attached Properties):**
    *   В `DashboardPanel.cs` зарегистрированы присоединенные свойства разметки: `ColProperty`, `RowProperty`, `SizeXProperty`, `SizeYProperty`.
    *   Кастомные методы `MeasureOverride` и `ArrangeOverride` полностью переписаны на чтение присоединенных свойств дочерних контролов вместо прямого доступа к вьюмоделям.
    *   В `DashboardView.axaml` в стиле `ContentPresenter` настроена двухсторонняя привязка этих свойств к координатам во вьюмоделях с использованием `x:DataType="vm:WidgetViewModelBase"`.
    *   Удален механизм ручной подписки на `PropertyChanged` у вьюмоделей виджетов. Обновление оверлеев теперь выполняется автоматически через `AddClassHandler` на уровне класса.
*   **Перевод телеметрии на Direct Properties для повышения производительности:**
    *   Свойства высокочастотной телеметрии `Setpoint` и `Feedback` (в `ValveControl.cs`), `IsFilled` (в `PipeControl.cs`) и `Values` (в `TrendLineControl.cs`) переведены из `StyledProperty` в `DirectProperty<T>` с приватными backing-полями. Это исключило аллокации и избыточные поиски в словарях при обновлении значений в реальном времени.
*   **Защита данных на этапе коэрсии (Coercion):**
    *   Реализована проверка и коэрсия (Coerce) значений в свойствах `CellWidth` и `CellHeight` (`[10.0, 500.0]`), а также `Thickness` у труб и клапанов (`[2.0, 50.0]`) и `MinY`/`MaxY` у трендов.
    *   В сеттерах Direct-свойств `Setpoint` и `Feedback` встроена безопасная коэрсия уставки и обратной связи в диапазоне `[0.0, 100.0]`.
*   **Устранение отладочного логирования:**
    *   Из `DashboardPanel.cs` удалено временное логирование в `debug_log.txt`.

---

## 📅 16.06.2026 (Сессия 9 — Выравнивание прилипания к визуальным фланцам)

### 📌 Достижение: Pixel-perfect привязка труб к графическим элементам Valve и Pump
*   **Метод GetVisualPortsInGrid:**
    *   Реализован расчет координат визуальных портов (`p1` и `p2`) в `DashboardPanel.cs` с учетом смещения самого графического элемента в контейнере (из-за DockPanel/текстовых надписей внизу), а также вращения и размера ячеек.
    *   Для `ValveControl` локальные координаты портов `(0, h/2)` и `(w, h/2)` вращаются тригонометрически вокруг центра элемента и переводятся с помощью `TranslatePoint` в систему координат панели, затем преобразуются в grid-координаты.
    *   Для `Pump` ( Viewbox) координаты вычисляются по границам круга насоса.
*   **Оптимизация snapping и rubber-banding:**
    *   В `OnPointerMoved` перетаскивание конца трубы теперь прилипает точно к рассчитанным визуальным фланцам.
    *   В `OnPointerReleased` и `OnPointerPressed` обновлена логика перемещения труб при драге задвижек: трубы смещаются строго по дельта-координатам виджета, устраняя избыточный расчет и повышая стабильность.
    *   Метод `GetSnappedPosition` адаптирован под новые относительные смещения визуальных портов.
*   **Сборка проекта:** Проект собирается без ошибок (`0 errors, 5 warnings`).

---

## 📅 16.06.2026 (Сессия 10 — Выделенный режим редактирования вершин труб)

### 📌 Достижение: Полное разделение перетаскивания вершин от перемещения трубы
*   **Управляемое состояние:**
    *   Во вьюмодель `PipeWidgetViewModel.cs` введено свойство `IsEditingVertices` и команда `ToggleEditingVerticesCommand`.
    *   В `PipeControl.cs` добавлено styled-свойство `IsEditingVertices` для синхронизации и принудительного вызова отрисовки.
*   **Условный рендеринг маркеров:**
    *   Маркеры-вершины (кружки) отрисовываются на трубе только при условии `IsDesignMode && IsEditingVertices`.
*   **Исключение конфликтов в DashboardPanel.cs:**
    *   В `OnPointerPressed` логика захвата и перетаскивания вершин, а также открытие контекстных меню вершин/сегментов теперь срабатывают **только** если режим `IsEditingVertices` активен на выбранной трубе. В противном случае перетаскивание мыши перемещает трубу целиком.
    *   В сеттере `SelectedVm` добавлена автодеактивация: при смене выделения (клике по фону или переключении на другой виджет) режим редактирования вершин у старой трубы автоматически выключается.
*   **Интеграция с меню в XAML:**
    *   В `DashboardView.axaml` в контекстное меню `DragHandle` для трубы добавлены кнопки переключения режима: `"Редактировать вершины"` (если выключено) и `"Завершить редактирование"` (если включено).
*   **Сборка проекта:** Проект собирается успешно (`0 ошибок, 5 предупреждений`).

---

## 📅 09.09.2026 (Сессия 11 — Кодекс разработки проекта и регламент архитектора)

### 📌 Достижение: Внедрение и формализация Кодекса разработки
*   **Сделано:** 
    *   Создан отдельный эталонный файл [docs/agent.md](agent.md) с полным текстом Кодекса разработки проекта (роль Senior C# / .NET / Avalonia Architect).
    *   Правила продублированы в каталог автоматического обнаружения правил среды Antigravity: [.agents/rules/agent.md](../.agents/rules/agent.md) и [AGENTS.md](../AGENTS.md).
    *   Обновлен главный [README.md](../README.md) с выделенным разделом ключевых принципов Кодекса.
    *   Обновлен регламент работы ИИ-ассистентов в [docs/README.md](README.md) с обязательным требованием соблюдения Кодекса.
    *   Проведено полное код-ревью решения с фиксацией точек роста (парсинг чисел в MQTT/контролах через `CultureInfo.InvariantCulture`, утилизация подписок при закрытии MDI-окон, оптимизация аллокаций в `Render`).
*   **Изменение ТЗ:** Утвержден Кодекс разработки как обязательный закон проектирования, рефакторинга и создания кода для любого ИИ-агента в проекте.
*   **Следующие шаги:** Исправление выявленных в ходе код-ревью замечаний (культуронезависимый парсинг, утилизация ресурсов) и реализация модуля IoT-ретрансляции (Modbus $\leftrightarrow$ MQTT Bridge).

---

## 📅 09.09.2026 (Сессия 12 — Устранение критических замечаний Код-Ревью согласно Кодексу)

### 📌 Достижение: Культуронезависимый парсинг телеметрии и корректный жизненный цикл MDI-окон
*   **Культуронезависимый парсинг (`CultureInfo.InvariantCulture`, `NumberStyles.Float`):**
    *   `TagValueConverter.cs`: в `ToBool` и `ToDouble` задействован `CultureInfo.InvariantCulture`, строковые сравнения переведены на `StringComparison.OrdinalIgnoreCase` (без аллокаций `ToLower()`).
    *   `MqttProtocolDriver.cs`: устранен критический баг парсинга float из MQTT payload на машинах с русской локалью Windows (`45.5` распознавалось как строка вместо числа). Сравнение "true"/"false" переведено на `OrdinalIgnoreCase`.
    *   `ModbusProtocolDriver.cs`: устранен опасный вызов `double.Parse(value.ToString() ?? "0")` без провайдера культуры (вызывавший `FormatException` при отправке команд управления в русской локали). Переведен на безопасный `TagValueConverter.ToDouble`.
    *   `MockProtocolDriver.cs`: приведен к `CultureInfo.InvariantCulture` при эмуляции записи уставок.
    *   `TrendLineControl.cs`: парсинг коллекций `Values` переведен на `NumberStyles.Float` и `CultureInfo.InvariantCulture`.
    *   `PumpWidgetViewModel.cs`: логика парсинга дискретных сигналов унифицирована через `TagValueConverter.ToBool`.
    *   `SetValueViewModel.cs`, `TankWidgetViewModel.cs`, `RealTimeTrendViewModel.cs`, `ValveControlPopupViewModel.cs`: устранены локалезависимые `TryParse` и `ToString()`, внедрены явные `NumberStyles.Float` и `CultureInfo.InvariantCulture`.
*   **Устранение утечки памяти MDI-окон (Lifecycle & Disposables):**
    *   `ChildWindowViewModel.cs`: реализован интерфейс `IDisposable`, освобождающий контент окна (`(Content as IDisposable)?.Dispose()`) и обнуляющий делегат `CloseAction`.
    *   `MainViewModel.cs`: в колбэк закрытия окна `childWindow.CloseAction` внедрен вызов `childWindow.Dispose()`, гарантирующий отписку от `TagUpdates` в Rx.NET при закрытии окон оператором в UI.
    *   `MainViewModel.Dispose()`: обеспечена полная очистка и утилизация всех активных окон `ActiveChildWindows`.
*   **Сборка и верификация:** 
    *   Проект успешно собирается (`dotnet build --no-restore` завершен с `0 ошибок`).
---

## 📅 09.09.2026 (Сессия 13 — Устранение предупреждений компилятора, оптимизация Render и декомпозиция DashboardPanel)

### 📌 Достижение: Полная ликвидация warning'ов компилятора, Zero-Allocation рендеринг и модульная разгрузка панели мнемосхемы
*   **Устранение предупреждений компилятора и NuGet:**
    *   `DialogService.cs`: устранены предупреждения CS8602 (разыменование вероятной пустой ссылки) при обращении к `childWindow.CloseCommand` внутри замыканий за счет локальной непустой переменной `targetWindow`.
    *   `MainViewModel.cs`: устранено предупреждение CS8604 при передаче аргумента `content` в конструктор `ChildWindowViewModel` за счет проверки на `null` и строгой типизации переменной `windowContent`.
    *   `AvaloniaApplication1.csproj`: обновлена ссылка пакета `MQTTnet` на версию `4.3.6.1152` для исключения предупреждения NU1603.
    *   Итог: сборка решения выполняется с 0 предупреждений компилятора C# (`0 ошибок, 0 предупреждений CS`).
*   **Оптимизация аллокаций в Render (Custom Controls Zero-Allocation):**
    *   `TrendLineControl.cs`: устранены постоянные аллокации сотен объектов в секунду на кадр (`new Pen`, `new SolidColorBrush`, `new DashStyle`, `new List<double>`, `new List<Point>`). Внедрено статическое кэширование перьев/кистей (`GridPen`, `DefaultLinePen`, `DefaultAreaBrush`) и переиспользование полей списков (`_valList`, `_points`).
    *   `ValveControl.cs`: вынесены в статические кэшированные поля все перья и кисти (`ValveBorderPen`, `BoxOutlinePen`, `CenterTrianglePen`, `RedAlarmBorderPen`, `BlackAlarmPen`, `FlangeBrush`, `FlangePen`, `FeedbackBarBgBrush`, `FeedbackBarBgPen`, `StandardGreyColor`).
*   **Изоляция Code-Behind (Раздел 7.3 Кодекса):**
    *   `DashboardViewModel.cs`: добавлен метод `AdjustZoom(double delta)`, инкапсулирующий логику изменения масштаба и границы `Math.Clamp(..., 0.5, 3.0)`.
    *   `DashboardView.axaml.cs`: в обработчике колеса мыши устранена прямая мутация свойства `viewModel.ZoomScale` из code-behind, вызов заменен на `viewModel.AdjustZoom(delta)`.
*   **Декомпозиция God Object `DashboardPanel.cs` (Разделы 1.1, 2.1 Кодекса):**
    *   Создан специализированный хелпер `Views/DashboardPanelHelpers/VisualPortHelper.cs`.
    *   В него перенесены рекурсивные методы поиска элементов в визуальном дереве Avalonia (`FindValveControlRecursive`, `FindViewboxRecursive`, `FindPipeControlRecursive`, `FindPipeControlsRecursive`, `FindAllSiblingPipesFor`, `FindDragHandleRecursive`, `IsDragHandle`), а также геометрический расчет графического центра и визуальных портов прилипания («резиновых труб», `GetGraphicsCenter`, `GetVisualPortsInGrid`).
    *   Размер `DashboardPanel.cs` сокращен почти на 300 строк.
*   **Сборка и тесты:**
    *   `dotnet build --no-restore` завершен успешно (0 ошибок, 0 предупреждений кода).
    *   `dotnet test --no-build` завершен успешно.

---

## 📅 09.09.2026 (Сессия 14 — Повторный аудит и Код-Ревью качества)

### 📌 Достижение: Комплексная повторная оценка кодовой базы по Кодексу разработки
*   **Динамика ключевых показателей:**
    *   Общий сводный балл проекта вырос с **~6.8/10** (до Сессий 12–13) до **8.0/10**.
    *   Предупреждения компилятора: **0 предупреждений** (полностью ликвидированы CS8602, CS8604, NU1603).
    *   Блокирующие вызовы потоков: **0 вызовов** (нет ни одного `.Result`, `.Wait()`, `GetAwaiter().GetResult()`).
    *   Утечки памяти окон: **0 утечек** (каскадная утилизация через `IDisposable` и `CloseAction`).
    *   Локалезависимость парсинга телеметрии: ликвидирована в конвертерах, драйверах и VM.
    *   Частичный Zero-Allocation рендеринг: успешно внедрен в `TrendLineControl` и `ValveControl`.
*   **Идентифицированные точки роста (Backlog задач):**
    1.  *Логирование (Раздел 12.2):* 17 прямых вызовов `Console.WriteLine` в сервисах и драйверах. Требуется внедрение `Microsoft.Extensions.Logging` и регистрация `ILogger` в DI.
    2.  *Обработка исключений (Раздел 12.1):* 4 пустых блока `catch { }` в `ModbusServerManager.cs` (строки 84, 94, 105) и `MockProtocolDriver.cs` (строка 140).
    3.  *Аллокации в рендеринге труб и оверлеев (Раздел 8.1):* `PipeControl.Render` создает `new StreamGeometry()`, `new LinearGradientBrush()`, `new List<Point>()` на каждый кадр; `GridOverlay` и `SelectionOverlay` создают кисти/перья при каждой отрисовке.
    4.  *Культуронезависимость в `MqttProtocolDriver.WriteAsync`:* преобразование `value.ToString()` без передачи `CultureInfo.InvariantCulture`.
    5.  *Модульные тесты (Раздел 13.1):* полное отсутствие проекта `AvaloniaApplication1.Tests`.

---

## 📅 09.09.2026 (Сессия 15 — Новые технологические аппараты SCADA, автотрассировка труб и фикс MQTT)

### 📌 Достижение: Аппараты мнемосхемы (Теплообменник, Реактор с мешалкой, Уровнемер), ортогональная автотрассировка труб (90°) и устранение бага MQTT

*   **Исправление культуронезависимости в MQTT (`MqttProtocolDriver.cs`):**
    *   В методе `WriteAsync` отправка уставки в MQTT переведена на строгое форматирование `formattable.ToString(null, CultureInfo.InvariantCulture)`. Это исключило подстановку запятой вместо точки в русской локали Windows (например, `"45,5"` $\to$ `"45.5"`), что гарантирует корректную передачу управляющих команд в брокеры и PLC.
*   **Сервис ортогональной автотрассировки трубопроводов (`Services/PipeAutoRouter.cs`):**
    *   Реализован специализированный A*-маршрутизатор на ортогональной дискретной сетке (Manhattan routing).
    *   Внедрен динамический штраф за повороты под 90° (`BendPenalty = 15.0`), обеспечивающий минимальное количество колен и красивую инженерную трассировку магистралей.
    *   Реализован учет габаритов технологических аппаратов мнемосхемы как непроходимых препятствий с защитным зазором (padding) и гарантированным подключением к целевым штуцерам/портам.
    *   Реализован алгоритм сжатия и очистки пути (`SimplifyCollinearPoints`), удаляющий избыточные промежуточные точки на прямых отрезках.
*   **Новые типы технологических аппаратов в конфигурации и сериализации:**
    *   `Models/Config/HmiConfiguration.cs`: добавлены строго типизированные классы `HeatExchangerConfig`, `ReactorConfig`, `LevelSensorConfig`.
    *   `Models/Config/WidgetJsonConverter.cs`: добавлена полиморфная JSON-десериализация новых аппаратов с сохранением обратной совместимости.
    *   `Models/Config/ConnectionType.cs`: расширен реестр `WidgetTypes.All` новыми типами (`HeatExchanger`, `Reactor`, `LevelSensor`).
*   **Векторные кастомные SCADA-контролы (Zero-Allocation рендеринг):**
    *   `Views/HeatExchangerControl.cs`: кожухотрубные и пластинчатые теплообменники, трубные пучки, внутренние перегородки (baffles), 4 патрубка контуров и индикаторы дельты температур.
    *   `Views/ReactorControl.cs`: вертикальный цилиндрический корпус с эллиптическими/скругленными днищами, рубашка нагрева/охлаждения (jacket), вертикальная шкала заполнения и анимированная мешалка/импеллер с питанием от `DispatcherTimer` (~30 FPS, активен строго при `IsAgitatorRunning = true`).
    *   `Views/LevelSensorControl.cs`: приборный круг стандарта ISA 5.1 (`LT`), монтажный фланец, измерительный волновод/радарные волны и цветовая индикация порогов Hi/Lo.
*   **Реактивные ViewModels и интеграция с редактором:**
    *   Разработаны `ViewModels/HeatExchangerWidgetViewModel.cs`, `ViewModels/ReactorWidgetViewModel.cs`, `ViewModels/LevelSensorWidgetViewModel.cs` с реактивной подпиской на теги через `TagManagerService` и троттлингом.
    *   Зарегистрированы в `ViewModels/WidgetFactory.cs`.
    *   В `ViewModels/WidgetEditorViewModel.cs` поддержаны конфигурационные панели для настройки габаритов, цветов и уставок новых аппаратов.
*   **Интеграция с панелью мнемосхемы (`DashboardPanel.cs`, `VisualPortHelper.cs`, `DashboardView.axaml`):**
    *   В `VisualPortHelper.cs` добавлены расчеты геометрических портов прилипания («резиновых труб») для теплообменников (4 штуцера) и реакторов (штуцеры загрузки, слива и теплоносителя).
    *   В `DashboardViewModel.cs` добавлена команда `AutoRoutePipeCommand(pipe)`.
    *   В контекстное меню трубы на мнемосхеме (`DashboardView.axaml`) добавлен пункт `«Автотрассировка (под 90°)»`.
*   **Демонстрационная технологическая цепочка:**
    *   В `config.json` добавлена готовая технологическая линия: Реактор R-101 $\to$ Датчик LT-101 $\to$ Линия перекачки $\to$ Теплообменник E-102.
*   **Сборка и запуск:**
    *   `dotnet build --no-restore` завершен успешно: **0 ошибок, 0 предупреждений CS**.
    *   Приложение успешно запущено и протестировано в рабочем окружении.

---

## 📅 10.09.2026 (Сессия 16 — Исправление хит-тестинга, удаления объектов и позиционирования контекстного меню)

### 📌 Достижение: Устранение багов смещения при выборе, ложного удаления предыдущих объектов и некорректного открытия ContextMenu

*   **Причины дефектов («смещение при выборе и удаление предыдущего»):**
    1.  *Прямоугольный хит-бокс труб (`Pipe`):* `PipeWidgetViewModel` наследовался от `WidgetViewModelBase`, и цикл поиска попадания проверял прямоугольный габарит `childBounds`. Если трасса трубы охватывала площадь 20x15 клеток, любой клик по аппаратам внутри этого прямоугольника ошибочно выбирал трубу вместо целевого аппарата.
    2.  *Прямой обход `Children` в порядке добавления (0 $\to$ N):* при перекрытии или соседстве элементов всегда захватывался нижний/предыдущий элемент коллекции вместо верхнего по Z-порядку.
    3.  *Игнорирование встроенного хит-тестинга Avalonia:* координатная математика не учитывала визуальное дерево `e.Source` и кастомный хит-тест трубы (`ICustomHitTest`).
    4.  *Рассинхрон координат `GetCol`:* при перемещении виджетов VM обновлялась сразу, но attached property `ColProperty` на `ContentPresenter` через стиль TwoWay-binding могло запаздывать, из-за чего хит-бокс оставался на старом (предыдущем) месте.
    5.  *Нулевой размер `DragHandle` при открытии меню:* у невыделенного аппарата `DragHandle.IsVisible = false`, его `Bounds = 0x0`. При вызове `ContextMenu.Open(dragHandle)` контекстное меню открывалось в точке `(0, 0)` со смещением.
*   **Реализованные исправления:**
    *   `DashboardPanel.cs`:
        *   Внедрен двухэтапный хит-тестинг: приоритетный поиск через визуальное дерево Avalonia (`e.Source` $\to$ `GetVisualParent() == this`) с проверкой `ICustomHitTest` для труб, и надежный fallback с обратным обходом `Children` от верхних к нижним (`Children.Count - 1` вниз до 0).
        *   Для труб проверка попадания переведена исключительно на сегменты (`IsPointNearSegment`), прямоугольный хит-бокс для труб полностью исключен.
        *   Координаты и размеры виджетов теперь считываются напрямую из свойств ViewModel (`vm.Col`, `vm.Row`, `vm.SizeX`, `vm.SizeY`), исключая любые задержки XAML-биндингов.
        *   При завершении перетаскивания/ресайза свойства `ColProperty`, `RowProperty`, `SizeXProperty`, `SizeYProperty` на `_dragChild` синхронизируются принудительно и синхронно (`SetCol`, `SetRow`, `SetSizeX`, `SetSizeY`).
        *   В `MeasureOverride` и `ArrangeOverride` координаты также считываются напрямую из `vm`.
        *   Контекстное меню теперь открывается строго по координатам мыши (`menu.Placement = PlacementMode.Pointer;`), с явной установкой `menu.DataContext = clickedVm`, гарантирующей передачу корректного объекта в `CommandParameter`.
        *   Добавлена поддержка удаления выбранного виджета клавишей `Delete` на клавиатуре в `OnKeyDown`.
    *   `DashboardViewModel.cs`:
        *   В команды `RemoveWidget` и `DuplicateWidget` добавлен защитный fallback `widget ??= Widgets.FirstOrDefault(w => w.IsSelected);`, предотвращающий холостые вызовы при отсутствующем параметре.
*   **Сборка и верификация:**
    *   `dotnet build --no-restore`: **0 ошибок, 0 предупреждений CS**.
    *   Приложение запущено и протестировано.

---

## 📅 10.09.2026 (Сессия 17 — Диагностика блокировки файлов при сборке и верификация)

### 📌 Достижение: Устранение блокировки бинарных файлов и подтверждение чистой сборки проекта

*   **Диагностика сбоя сборки:**
    *   В предыдущей сессии приложение было запущено в фоновом режиме через `dotnet run` для визуального тестирования.
    *   В операционной системе Windows запущенный процесс приложения удерживает исключительную файловую блокировку на выходные сборки (`AvaloniaApplication1.exe` и `AvaloniaApplication1.dll` в каталоге `bin/Debug/net8.0/`).
    *   При попытке пересобрать проект через Visual Studio или команду `dotnet build` компилятор/MSBuild не мог перезаписать целевой файл и завершался ошибкой `MSB3027` / `MSB3021` (*«Процесс не может получить доступ к файлу, так как этот файл занят другим процессом»*).
*   **Устранение и верификация:**
    *   Фоновые процессы приложения были корректно завершены, файловые блокировки сняты.
    *   Выполнена чистая сборка решения в конфигурации `Release`: **0 ошибок, 0 предупреждений**.
    *   Выполнена чистая сборка решения в конфигурации `Debug`: **0 ошибок, 0 предупреждений**.

---

## 📅 11.09.2026 (Сессия 18 — Перехватчик событий мыши в Tunnel-режиме и надежное нативное ContextMenu удаления)

### 📌 Достижение: Полная ликвидация бага смещения при выборе, перехвата кликов кнопками внутри виджетов и ошибочного удаления контролов

*   **Анализ первопричин дефекта:**
    1.  *Перехват `PointerPressed` интерактивными контролами (Buttons, Switches):* В `DashboardView.axaml` аппараты вроде задвижек (`Valve`) и насосов (`Pump`) были обернуты в растянутый на весь габарит `Button Command="{Binding OpenControlPopupCommand}"`. До выбора элемента оверлей `DragHandle` скрыт (`IsVisible = false`), поэтому клик попадал прямо в кнопку Avalonia. Кнопка устанавливала `e.Handled = true`, и `DashboardPanel.OnPointerPressed` сразу завершал выполнение (`if (e.Handled) return;`), не выбирая элемент. В результате оставался выбранным предыдущий элемент, и контекстное меню ошибочно удаляло его.
    2.  *Нестабильность XAML-привязок в контекстном меню:* В 15 DataTemplates дублировались блоки `<Border.ContextMenu>` со сложными цепочками `{Binding $parent[ContextMenu].PlacementTarget.Tag.RemoveWidgetCommand}`. В рантайме Avalonia при динамическом открытии меню `PlacementTarget` или `Tag` часто оставались `null`, из-за чего клик по «Удалить» либо не вызывал никакой команды, либо падал в фоллбэк `Widgets.FirstOrDefault(w => w.IsSelected)`.
    3.  *Удержание устаревшей ссылки на удаленный виджет:* При удалении виджета свойство `SelectedVm` в `DashboardPanel` не очищалось синхронно, а `widget.IsSelected` оставался `true`.
*   **Реализованные архитектурные решения:**
    *   **Перехват в режиме Tunnel (`RoutingStrategies.Tunnel`):**
        *   В `DashboardPanel` зарегистрирован обработчик `AddHandler(PointerPressedEvent, OnPreviewPointerPressed, RoutingStrategies.Tunnel)`.
        *   В режиме редактирования (`IsDesignMode == true`) панель получает событие клика **до** того, как оно дойдет до дочерних кнопок (`Button`, `ToggleSwitch`, `Slider`).
        *   Кнопки больше не поглощают клик и не открывают окна управления при редактировании схемы. Любой виджет мгновенно и надежно выбирается с первого клика.
    *   **Единое программное контекстное меню (`ShowWidgetContextMenu`):**
        *   Из `DashboardView.axaml` полностью удалены все 15 дублированных блоков `<Border.ContextMenu>` (файл сокращен на 249 строк избыточного кода).
        *   В `DashboardPanel.cs` реализован метод `ShowWidgetContextMenu(WidgetViewModelBase targetVm, Control? targetChild)`, создающий нативное меню Avalonia с прямыми C#-делегатами к командам `DashboardViewModel` (`RemoveWidgetCommand`, `DuplicateWidgetCommand`, `EditWidgetCommand`, `BringToFrontCommand` и др.).
        *   При клике «Удалить» вызывается строго `dashboardVm.RemoveWidgetCommand.Execute(targetVm)`, гарантируя удаление именно того объекта, для которого было открыто меню.
    *   **Корректный сброс состояния:**
        *   При удалении виджета сразу сбрасываются `SelectedVm = null` и `widget.IsSelected = false`.
        *   При выходе из режима редактирования (`!IsDesignMode`) активный выбор и открытые меню автоматически закрываются.
*   **Верификация:**
    *   `dotnet build` в конфигурациях Debug и Release: **0 ошибок, 0 предупреждений**.
    *   Все тесты решения пройдены успешно.

---

## 📅 11.09.2026 (Сессия 19 — Аудит правил, стандарты Avalonia 11+, Headless UI Runner и Visual Protocol)

### 📌 Достижение: Внедрение кодекса разработки Avalonia 11+, автономного Headless-раннера UI и протокола визуальной верификации

*   **Аудит и консолидация правил разработки:**
    *   Проведен глубокий аудит существующих инструкций (`docs/agent.md`, `.agents/rules/agent.md`, `AGENTS.md`, `GEMINI.md`, `docs/preprompt.md`).
    *   Все правила сохранены без потерь контекста (архитектура MDI, SCADA мнемосхема 10x10, ISA-виджеты, Tunnel routing, аппаратные драйверы Modbus/MQTT).
    *   Добавлены стандарты разработки под Avalonia 11+ и C# 12:
        *   Специфика свойств Avalonia (`StyledProperty<T>`, `DirectProperty<TOwner, TValue>`, `AffectsRender`, `AffectsMeasure`).
        *   Avalonia XAML vs WPF: запрет `DependencyProperty.Register`, `<Style.Triggers>` и `MultiDataTrigger`; использование селекторов псевдоклассов `^:pointerover`, `^:pressed`, `^:disabled`.
        *   Compiled Bindings: обязательное указание `x:DataType`, запрет отключения `CompileBindings="False"`, типизация контекстов данных.
        *   Рендеринг и перформанс: нулевые аллокации в `Render(DrawingContext)` (кэширование `Pen`, `Brush`, `Geometry`), отсутствие блокировок UI потока, передача неизменяемых структур данных.
        *   Современный MVVM: использование `[ObservableProperty]`, `[RelayCommand]` из `CommunityToolkit.Mvvm`, запрет самодельного boilerplate-кода.
    *   Созданы и актуализированы единые файлы: `.antigravity/rules.md`, `AGENT_RULES.md`, `.editorconfig`, `AGENTS.md`, `GEMINI.md`, `docs/agent.md`, `.agents/rules/agent.md`.
*   **Реализация Headless UI Runner (`AvaloniaApplication1.UIValidation`):**
    *   Развернут отдельный CLI-проект `AvaloniaApplication1.UIValidation` на базе `Avalonia.Headless` + `Avalonia.Skia`.
    *   Внедрена безопасная изоляция от сетевых сокетов: при запуске в headless-режиме приложение не открывает блокирующие TCP-сокеты брокера MQTT (`stp10:1883`) и Modbus PLC (`plc1`), используя моки сервисов данных при сохранении реальных виджетов, стилей и шаблонов дашборда.
    *   Решена проблема зависания `dotnet test` (вызванная перехватом локальных сокетов VSTest прокси-переменными окружения) путем прямого автономного запуска CLI (`dotnet run --project AvaloniaApplication1.UIValidation`).
    *   Реализована выгрузка визуального состояния интерфейса: скриншот в формате PNG (`artifacts/ui_preview.png`) и структурное дерево элементов в JSON (`artifacts/ui_tree.json`).
*   **Скрипты автоматизации и проверки:**
    *   Созданы кроссплатформенные скрипты запуска:
        *   `scripts/render_ui.sh` и `scripts/render_ui.ps1` (генерация скриншота и JSON-дерева за 3-4 секунды).
        *   `scripts/check-build.sh` и `scripts/check-build.ps1` (чистая сборка с расширенной диагностикой XAMLIL AvaloniaShowTrace).
*   **Верификация:**
    *   Рендеринг `MainWindow` выполнен штатно за 3.8 сек без зависаний.
    *   Сформированы артефакты `artifacts/ui_preview.png` (38.7 КБ) и `artifacts/ui_tree.json` (354.7 КБ).
    *   Сборка решения `dotnet build AvaloniaApplication1.sln`: **0 предупреждений, 0 ошибок**.

---

## 📅 11.09.2026 (Сессия 20 — Быстрая магнитная стыковка труб и клапанов с плотной геометрией фланцев)

### 📌 Достижение: Реализация геометрически точной стыковки встык (Вариант 2), расширение магнитного захвата до 25 px, неоновый snap-индикатор и автоматическая установка фланцев

*   **Анализ требований и геометрическая модель:**
    *   *Требование:* Фланец трубы должен вплотную прилегать к визуальному фланцу задвижки (основанию треугольника bowtie) с 0 px зазора и 0 px врезания в тело клапана.
    *   *Выбранный вариант (Вариант 2):* Одиночный фланец на конце трубы, прилегающий встык к основанию треугольника клапана без двойных накладок.
    *   *Математика фланца:* В `PipeControl.DrawFlangePlate` внешняя плоскость пластины выровнена ровно по координате порта $P$, а толщина пластины уходит строго назад вдоль вектора подходящего сегмента трубы.
    *   *Смещение цилиндра трубы:* Сегменты объемной трубы 3D (`seg0Start` и `segLastEnd`) сдвинуты назад на `flangeThickness`, исключая просвечивание трубы сквозь фланец и задвижку.
    *   *Очистка треугольников клапана:* В `ValveControl` свойство `ShowFlangesProperty` по умолчанию отключено (`false`), обеспечивая чистое сопряжение трубы с основанием треугольника.
*   **Коррекция портов задвижек (`VisualPortHelper`):**
    *   Учтена высота регулирующего клапана при отображении шкалы обратной связи: `availableH = isRegulating && showFeedbackBar ? h - 18 : h`.
    *   Координаты портов рассчитываются строго в центрах оснований треугольников с учетом поворота (`IsVertical`).
*   **Магнитный захват и интерактивный отклик (UX):**
    *   Радиус примагничивания портов увеличен до 25 px.
    *   В `DashboardPanel` добавлены `ActiveSnapTarget` и флаг `_isSnappedToEquipmentPort`.
    *   В `SelectionOverlay` реализован яркий неоново-зеленый snap-индикатор (`Color.FromRgb(0, 230, 118)`, радиус 11 px с белой точкой 3.5 px), подсвечивающий целевой порт в реальном времени при наведении курсора.
    *   При отпускании мыши (`OnPointerReleased`) труба автоматически получает свойство `StartFitting = "Flange"` (для начала) или `EndFitting = "Flange"` (для конца).
*   **Поддержка форматов координат:**
    *   В `PipeControl.ParsePoints` и `PipeWidgetViewModel.GetAbsoluteGridPoints` добавлена поддержка координат, разделенных как пробелами, так и точками с запятой.
*   **Визуальная валидация (Headless UI Runner):**
    *   В `AvaloniaApplication1.UIValidation` добавлен рендеринг мнемосхемы (`MimicView`).
    *   Сгенерирован снимок `artifacts/ui_preview.png`: визуально подтверждено безупречное плотное прилегание фланцев труб к треугольникам клапана `YV1` без наложений и зазоров.
    *   Сборка решения `scripts/check-build.ps1`: **0 ошибок, 0 предупреждений**.

---

## 2026-09-14 вЂ” РЎРµСЃСЃРёСЏ 21: Р›РѕРєР°Р»РёР·Р°С†РёСЏ СЃРјРµС‰РµРЅРёСЏ Рё СЃС‚С‹РєРѕРІРєР° С‚СЂСѓР± СЃ С‚СЂРµСѓРіРѕР»СЊРЅРёРєР°РјРё РєР»Р°РїР°РЅР° (Zero-Gap Flush Docking)

### рџЋЇ Р¦РµР»СЊ СЃРµСЃСЃРёРё
РЈСЃС‚СЂР°РЅРёС‚СЊ РІРёР·СѓР°Р»СЊРЅРѕРµ РЅРµСЃРѕРІРїР°РґРµРЅРёРµ (В«РІРёРґРёС€СЊ С‚СЂСѓР±Р° РЅРµ СЃС‚С‹РєСѓРµС‚СЃСЏ СЃ С‚СЂРµСѓРіРѕР»СЊРЅРёРєРѕРј РєР»Р°РїР°РЅР°В»), РіРґРµ С‚СЂСѓР±Р° РїСЂРѕС…РѕРґРёР»Р° РЅРёР¶Рµ С‚СЂРµСѓРіРѕР»СЊРЅРёРєРѕРІ РєР»Р°РїР°РЅР° Рё РёРјРµР»Р° СЃРґРІРёРі РїРѕ РіРѕСЂРёР·РѕРЅС‚Р°Р»Рё.

### рџ”Ќ РђРЅР°Р»РёР· РєРѕСЂРЅРµРІС‹С… РїСЂРёС‡РёРЅ (Root Cause Analysis)
1. **РСЃРєР°Р¶РµРЅРёРµ РєРѕРјРїРѕРЅРѕРІРєРё DataTemplate РєР»Р°РїР°РЅР° (`DashboardView.axaml`):**
   * Р’ С€Р°Р±Р»РѕРЅРµ `ValveWidgetViewModel` РєРѕРЅС‚СЂРѕР» `ValveControl` РЅР°С…РѕРґРёР»СЃСЏ РІРЅСѓС‚СЂРё `DockPanel` РІРјРµСЃС‚Рµ СЃ Р±Р»РѕРєРѕРј Р·Р°РіРѕР»РѕРІРєР° Рё Р·РЅР°С‡РµРЅРёСЏ (`StackPanel DockPanel.Dock="Bottom"`), РѕС‚РЅРёРјР°РІС€РёРј ~35 px СЃРЅРёР·Сѓ.
   * Р­С‚Рѕ СЃР¶РёРјР°Р»Рѕ `ValveControl` РґРѕ РІС‹СЃРѕС‚С‹ 25 px РІРјРµСЃС‚Рѕ РїРѕР»РѕР¶РµРЅРЅС‹С… 60 px Рё Р·Р°РґРёСЂР°Р»Рѕ С†РµРЅС‚СЂ В«Р±Р°Р±РѕС‡РєРёВ» РєР»Р°РїР°РЅР° РЅР°РІРµСЂС… ($Y = 342.5$ px РІРјРµСЃС‚Рѕ $Y = 360$ px).
2. **РџРѕР»СѓСЏС‡РµРµС‡РЅС‹Р№ СЃРґРІРёРі СЃРµС‚РєРё (Half-cell Grid Offset):**
   * Р’ `PipeControl.Render` РїРёРєСЃРµР»СЊРЅС‹Рµ РєРѕРѕСЂРґРёРЅР°С‚С‹ СЂР°СЃСЃС‡РёС‚С‹РІР°СЋС‚СЃСЏ СЃРѕ СЃРјРµС‰РµРЅРёРµРј РЅР° С†РµРЅС‚СЂ СЏС‡РµР№РєРё: P = gp * S + S/2. Р”Р»СЏ S = 10: P = gp * 10 + 5.
   * Р’ `config.json` С‚СЂСѓР±С‹ Р±С‹Р»Рё Р·Р°С„РёРєСЃРёСЂРѕРІР°РЅС‹ РІ С†РµР»РѕС‡РёСЃР»РµРЅРЅС‹С… СЏС‡РµР№РєР°С… 30 Рё 36 ($X_{pixel} = 305$ Рё $X_{pixel} = 365$, $Y_{pixel} = 365$), РІ С‚Рѕ РІСЂРµРјСЏ РєР°Рє РіСЂР°РЅРё РєР»Р°РїР°РЅР° РЅР°С…РѕРґСЏС‚СЃСЏ РЅР° РіСЂР°РЅРёС†Р°С… СЏС‡РµРµРє ($X = 300$ Рё $X = 360$, С†РµРЅС‚СЂР°Р»СЊРЅР°СЏ РѕСЃСЊ $Y = 360$).
   * Р’ СЂРµР·СѓР»СЊС‚Р°С‚Рµ Р»РµРІР°СЏ С‚СЂСѓР±Р° РІСЂРµР·Р°Р»Р°СЃСЊ РЅР° 5 px РІРЅСѓС‚СЂСЊ РєР»Р°РїР°РЅР°, РїСЂР°РІР°СЏ РѕС‚СЃС‚РѕСЏР»Р° СЃ 5 px Р·Р°Р·РѕСЂРѕРј, Рё РѕР±Рµ РїСЂРѕС…РѕРґРёР»Рё РЅР° 5 px РЅРёР¶Рµ С‚СЂРµСѓРіРѕР»СЊРЅРёРєРѕРІ.
3. **РќРµС‚РѕС‡РЅРѕСЃС‚Рё fallback-СЂР°СЃС‡РµС‚Р° РІ `VisualPortHelper.cs`:**
   * Fallback-РєРѕРѕСЂРґРёРЅР°С‚С‹ РґР»СЏ РєР»Р°РїР°РЅР° Рё Р°РїРїР°СЂР°С‚РѕРІ РЅРµ СѓС‡РёС‚С‹РІР°Р»Рё РІС‹С‡РёС‚Р°РЅРёРµ РїРѕР»РѕРІРёРЅС‹ СЏС‡РµР№РєРё (-0.5), РІС‹РґР°РІР°СЏ X = col РІРјРµСЃС‚Рѕ col - 0.5.

### рџ› пёЏ Р’С‹РїРѕР»РЅРµРЅРЅС‹Рµ РёРЅР¶РµРЅРµСЂРЅС‹Рµ СЂРµС€РµРЅРёСЏ
1. **РџРµСЂРµСЃС‚СЂРѕР№РєР° РєРѕРјРїРѕРЅРѕРІРєРё РєР»Р°РїР°РЅР° (`DashboardView.axaml`):**
   * `DockPanel` Р·Р°РјРµРЅРµРЅ РЅР° РїРѕР»РЅРѕСЂР°Р·РјРµСЂРЅС‹Р№ `Grid`. `ValveControl` С‚РµРїРµСЂСЊ Р·Р°РЅРёРјР°РµС‚ РІСЃСЋ РїР»РѕС‰Р°РґСЊ РІРёРґР¶РµС‚Р° (60x60 px), РїРѕР·РёС†РёРѕРЅРёСЂСѓСЏ В«Р±Р°Р±РѕС‡РєСѓВ» СЃС‚СЂРѕРіРѕ РїРѕ С†РµРЅС‚СЂСѓ ($cy = 30$, $Y_{panel} = 360$ px).
   * Р‘Р»РѕРє Р·Р°РіРѕР»РѕРІРєР° Рё Р·РЅР°С‡РµРЅРёСЏ РІС‹РЅРµСЃРµРЅ РІ РЅРёР¶РЅРёР№ РѕРІРµСЂР»РµР№ (`Margin="0,0,0,-24"`) СЃ РѕС‚РєР»СЋС‡РµРЅРЅС‹Рј С…РёС‚-С‚РµСЃС‚РѕРј (`IsHitTestVisible="False"`), РёСЃРєР»СЋС‡РёРІ СЃР¶Р°С‚РёРµ РіРµРѕРјРµС‚СЂРёРё РєР»Р°РїР°РЅР°.
2. **РЎРёРЅС…СЂРѕРЅРёР·Р°С†РёСЏ С„РѕСЂРјСѓР» РїРѕСЂС‚РѕРІ РІ `VisualPortHelper.cs`:**
   * Fallback-РєРѕРѕСЂРґРёРЅР°С‚С‹ СЃРєРѕСЂСЂРµРєС‚РёСЂРѕРІР°РЅС‹ СЃ СѓС‡РµС‚РѕРј СЃРјРµС‰РµРЅРёСЏ -0.5 СЏС‡РµР№РєРё.
   * РџРѕСЂС‚С‹ Р°РїРїР°СЂР°С‚РѕРІ `HeatExchanger` Рё `Reactor` СЃРѕРіР»Р°СЃРѕРІР°РЅС‹ СЃ РїРѕР»СѓСЏС‡РµРµС‡РЅС‹Рј РїСЂР°РІРёР»РѕРј `PipeControl`.
3. **РўРѕС‡РЅР°СЏ РєР°Р»РёР±СЂРѕРІРєР° РєРѕРѕСЂРґРёРЅР°С‚ РІ `config.json` Рё `AvaloniaApplication1/config.json`:**
   * `Р›РёРЅРёСЏ R101 -> YV1`: `Position: { Row: 29.5, Col: 18.5, SizeX: 12, SizeY: 7 }`, С‚РѕС‡РєРё `"0,0;0,6;11,6"`. РќР°С‡Р°Р»Рѕ вЂ” СЃС‚СЂРѕРіРѕ С€С‚СѓС†РµСЂ СЂРµР°РєС‚РѕСЂР° X=190, Y=300; РїРѕРІРѕСЂРѕС‚ X=190, Y=360; РєРѕРЅРµС† вЂ” С‚РѕС‡РЅРѕ РѕСЃРЅРѕРІР°РЅРёРµ Р»РµРІРѕРіРѕ С‚СЂРµСѓРіРѕР»СЊРЅРёРєР° РєР»Р°РїР°РЅР° X=300, Y=360.
   * `Р›РёРЅРёСЏ YV1 -> E102`: `Position: { Row: 23.5, Col: 35.5, SizeX: 11, SizeY: 13 }`, С‚РѕС‡РєРё `"0,12;10,12;10,0"`. РќР°С‡Р°Р»Рѕ вЂ” С‚РѕС‡РЅРѕ РѕСЃРЅРѕРІР°РЅРёРµ РїСЂР°РІРѕРіРѕ С‚СЂРµСѓРіРѕР»СЊРЅРёРєР° РєР»Р°РїР°РЅР° X=360, Y=360; РїРѕРІРѕСЂРѕС‚ X=460, Y=360; РєРѕРЅРµС† вЂ” С€С‚СѓС†РµСЂ С‚РµРїР»РѕРѕР±РјРµРЅРЅРёРєР° X=460, Y=240.

### рџ§Є Р’РµСЂРёС„РёРєР°С†РёСЏ Рё СЂРµР·СѓР»СЊС‚Р°С‚С‹
* **Р’РёР·СѓР°Р»СЊРЅС‹Р№ Vision-Р°РЅР°Р»РёР· (`scripts/render_ui.ps1 MimicView`):**
  * РЎРіРµРЅРµСЂРёСЂРѕРІР°РЅ Р°СЂС‚РµС„Р°РєС‚ `artifacts/ui_preview.png` Рё РґРµС‚Р°Р»СЊРЅС‹Р№ СЃСЂРµР· `artifacts/valve_zoom2.png`.
  * Р’РёР·СѓР°Р»СЊРЅРѕ РїРѕРґС‚РІРµСЂР¶РґРµРЅР° 100% СЃРѕРѕСЃРЅРѕСЃС‚СЊ С‚СЂСѓР±С‹ Рё РєР»Р°РїР°РЅР° РїРѕ Р»РёРЅРёРё Y = 360.
  * Р¤Р»Р°РЅС†С‹ С‚СЂСѓР± РїСЂРёР»РµРіР°СЋС‚ Рє С‚СЂРµСѓРіРѕР»СЊРЅРёРєР°Рј РєР»Р°РїР°РЅР° РІРїР»РѕС‚РЅСѓСЋ: Р·Р°Р·РѕСЂ **0 px**, РЅР°С…Р»РµСЃС‚ **0 px**.
* **РЎР±РѕСЂРєР° (`scripts/check-build.ps1`):**
  * РЈСЃРїРµС€РЅР°СЏ СЃР±РѕСЂРєР° РІСЃРµРіРѕ СЂРµС€РµРЅРёСЏ: **0 РѕС€РёР±РѕРє, 0 РїСЂРµРґСѓРїСЂРµР¶РґРµРЅРёР№**.
---

## 2026-09-14 вЂ” РЎРµСЃСЃРёСЏ 22: Р РµР°Р»РёР·Р°С†РёСЏ РѕР±СЂР°С‚РЅРѕРіРѕ С„СѓРЅРєС†РёРѕРЅР°Р»Р° РїРѕР·РёС†РёРѕРЅРёСЂРѕРІР°РЅРёСЏ РєР»Р°РїР°РЅР° Рє РІРµСЂС€РёРЅРµ С‚СЂСѓР±С‹ (Reverse Port Snapping)

### рџЋЇ Р¦РµР»СЊ СЃРµСЃСЃРёРё
Р РµР°Р»РёР·РѕРІР°С‚СЊ РѕР±СЂР°С‚РЅРѕРµ РїСЂРёРјР°РіРЅРёС‡РёРІР°РЅРёРµ: РїСЂРё РїРµСЂРµС‚Р°СЃРєРёРІР°РЅРёРё РєР»Р°РїР°РЅР° (РёР»Рё РґСЂСѓРіРѕРіРѕ С‚РµС…РЅРѕР»РѕРіРёС‡РµСЃРєРѕРіРѕ Р°РїРїР°СЂР°С‚Р° СЃ РїРѕСЂС‚Р°РјРё) РєР»Р°РїР°РЅ РґРѕР»Р¶РµРЅ Р°РІС‚РѕРјР°С‚РёС‡РµСЃРєРё Рё РїР»РѕС‚РЅРѕ РїСЂРёР»РёРїР°С‚СЊ СЃРІРѕРёРјРё РїРѕСЂС‚Р°РјРё (РѕСЃРЅРѕРІР°РЅРёСЏРјРё С‚СЂРµСѓРіРѕР»СЊРЅРёРєРѕРІ) Рє РєРѕРЅС†РµРІС‹Рј РІРµСЂС€РёРЅР°Рј СЃСѓС‰РµСЃС‚РІСѓСЋС‰РёС… С‚СЂСѓР± РЅР° РјРЅРµРјРѕСЃС…РµРјРµ.

### рџ”Ќ РђСЂС…РёС‚РµРєС‚СѓСЂРЅРѕРµ СЂРµС€РµРЅРёРµ
1. **Р Р°СЃС‡РµС‚ Р»РѕРєР°Р»СЊРЅС‹С… СЃРјРµС‰РµРЅРёР№ РїРѕСЂС‚РѕРІ ($relX, relY$):**
   * Р”Р»СЏ РїРµСЂРµРјРµС‰Р°РµРјРѕРіРѕ РІРёРґР¶РµС‚Р° С‡РµСЂРµР· `VisualPortHelper.GetVisualPortsInGrid` РІС‹С‡РёСЃР»СЏСЋС‚СЃСЏ РѕС‚РЅРѕСЃРёС‚РµР»СЊРЅС‹Рµ РєРѕРѕСЂРґРёРЅР°С‚С‹ РµРіРѕ РїРѕСЂС‚РѕРІ:
     $relX_1 = p1_{grid}.X - col, \quad relY_1 = p1_{grid}.Y - row$
     $relX_2 = p2_{grid}.X - col, \quad relY_2 = p2_{grid}.Y - row$
2. **РЎРєР°РЅРёСЂРѕРІР°РЅРёРµ РєСЂР°Р№РЅРёС… РІРµСЂС€РёРЅ С‚СЂСѓР±:**
   * РЎРєР°РЅРёСЂСѓСЋС‚СЃСЏ РІСЃРµ С‚СЂСѓР±С‹ РЅР° РїР°РЅРµР»Рё (`DataContext is PipeWidgetViewModel`).
   * РСЃРєР»СЋС‡Р°СЋС‚СЃСЏ С‚СЂСѓР±С‹, СѓР¶Рµ РІС…РѕРґСЏС‰РёРµ РІ `_connectedPipePoints` (РєРѕС‚РѕСЂС‹Рµ С‚СЏРЅСѓС‚СЃСЏ РІРјРµСЃС‚Рµ СЃ РІРёРґР¶РµС‚РѕРј В«СЂРµР·РёРЅРѕРІРѕР№ СЃРІСЏР·СЊСЋВ»).
   * Р”Р»СЏ РєР°Р¶РґРѕРіРѕ РїРѕСЂС‚Р° РІРёРґР¶РµС‚Р° Рё РєСЂР°Р№РЅРёС… С‚РѕС‡РµРє С‚СЂСѓР±С‹ (РЅР°С‡Р°Р»Рѕ $pts[0]$ Рё РєРѕРЅРµС† $pts[n-1]$) РїСЂРѕРІРµСЂСЏРµС‚СЃСЏ СЂР°СЃСЃС‚РѕСЏРЅРёРµ РІ РїРёРєСЃРµР»СЏС…:
     $dist = \sqrt{((port_{abs}.X - P_{pipe}.X) \cdot CellWidth)^2 + ((port_{abs}.Y - P_{pipe}.Y) \cdot CellHeight)^2}$
3. **Р—Р°С…РІР°С‚ РїРѕСЂС‚Р° (Capture Radius = 25 px):**
   * РџСЂРё РїРѕРїР°РґР°РЅРёРё РІ СЂР°РґРёСѓСЃ Р·Р°С…РІР°С‚Р° РїРѕР·РёС†РёСЏ РІРёРґР¶РµС‚Р° РІС‹СЂР°РІРЅРёРІР°РµС‚СЃСЏ:
     $snappedCol = P_{pipe}.X - relX_{port}$
     $snappedRow = P_{pipe}.Y - relY_{port}$
   * Р’РєР»СЋС‡Р°РµС‚СЃСЏ РїРѕРґСЃРІРµС‚РєР° `ActiveSnapTarget` вЂ” РЅРµРѕРЅРѕРІРѕ-Р·РµР»РµРЅРѕРµ РєРѕР»СЊС†Рѕ СЃ Р±РµР»РѕР№ С‚РѕС‡РєРѕР№ РІ `SelectionOverlay`.
4. **Р¤РёРєСЃР°С†РёСЏ Рё Р°РІС‚РѕС„Р»Р°РЅРµС† (`OnPointerReleased`):**
   * РџСЂРё РѕС‚РїСѓСЃРєР°РЅРёРё РјС‹С€Рё РІРёРґР¶РµС‚ С„РёРєСЃРёСЂСѓРµС‚СЃСЏ РІ С‚РѕС‡РЅС‹С… РєРѕРѕСЂРґРёРЅР°С‚Р°С… СЃС‚С‹РєРѕРІРєРё ($newCol, newRow$).
   * РљРѕРЅРµС† С‚СЂСѓР±С‹ Р°РІС‚РѕРјР°С‚РёС‡РµСЃРєРё РїРѕР»СѓС‡Р°РµС‚ С‚РёРї С„РёС‚РёРЅРіР° `"Flange"` (`StartFitting = "Flange"` РёР»Рё `EndFitting = "Flange"`), РµСЃР»Рё С‚Р°Рј Р±С‹Р»Рѕ `"None"`.

### рџ§Є Р’РµСЂРёС„РёРєР°С†РёСЏ
* **РЎР±РѕСЂРєР° (`scripts/check-build.ps1`):** **0 РѕС€РёР±РѕРє, 0 РїСЂРµРґСѓРїСЂРµР¶РґРµРЅРёР№**.
* **Headless UI Runner (`scripts/render_ui.ps1 MimicView`):** РЈСЃРїРµС€РЅРѕ СЃРіРµРЅРµСЂРёСЂРѕРІР°РЅС‹ Р°СЂС‚РµС„Р°РєС‚С‹ `ui_preview.png` Рё `ui_tree.json`.
---

## 2026-09-14 вЂ” РЎРµСЃСЃРёСЏ 23: РРЅС‚РµРіСЂР°С†РёСЏ СЃ РїСЂРѕРµРєС‚РѕРј РџР›Рљ 511-05 Рё РєРѕРЅС„РёРіСѓСЂР°С†РёСЏ HMI РђРїРїР°СЂР°С‚Р° 511

### рџЋЇ Р¦РµР»СЊ СЃРµСЃСЃРёРё
РќР° РѕСЃРЅРѕРІРµ РїСЂРµРґРѕСЃС‚Р°РІР»РµРЅРЅРѕРіРѕ СЂРµРїРѕР·РёС‚РѕСЂРёСЏ РџР›Рљ (https://github.com/serresident/511-05.git) СЂР°Р·СЂР°Р±РѕС‚Р°С‚СЊ РїРѕР»РЅРѕС†РµРЅРЅСѓСЋ РєРѕРЅС„РёРіСѓСЂР°С†РёСЋ HMI SCADA РґР»СЏ РјРѕРЅРёС‚РѕСЂРёРЅРіР° Рё РІРµРґРµРЅРёСЏ С‚РµС…РЅРѕР»РѕРіРёС‡РµСЃРєРѕРіРѕ РїСЂРѕС†РµСЃСЃР° РђРїРїР°СЂР°С‚Р° 511 (С†РµС… 15, РѕС‚РґРµР»РµРЅРёРµ Р°Р·РѕСЃРѕС‡РµС‚Р°РЅРёСЏ).

### рџ”Ќ РСЃСЃР»РµРґРѕРІР°РЅРёРµ Рё Р°РЅР°Р»РёР· РїСЂРѕРµРєС‚Р° РџР›Рљ
1. **РЎСЂРµРґР° Рё Р°СЂС…РёС‚РµРєС‚СѓСЂР° РџР›Рљ:**
   * Abak.IDE V1.0.0 (CODESYS V3.5), Structured Text (.st), Р·Р°РґР°С‡Р° `HmiCommunication` (100 РјСЃ, РїСЂРёРѕСЂРёС‚РµС‚ 21).
   * Modbus TCP Slave РЅР° РїРѕСЂС‚Сѓ 502, Р°РґСЂРµСЃР°С†РёСЏ СЂРµРіРёСЃС‚СЂРѕРІ СЃРѕ СЃРјРµС‰РµРЅРёСЏРјРё: Coils (0), Discrete Inputs (499), Holding Registers (100), Input Registers (0).
   * РџРѕСЂСЏРґРѕРє Р±Р°Р№С‚ РІ 32-Р±РёС‚РЅС‹С… С‡РёСЃР»Р°С… СЃ РїР»Р°РІР°СЋС‰РµР№ С‚РѕС‡РєРѕР№: `CDAB` (Little-Endian Word Swap).
2. **РўРµС…РЅРѕР»РѕРіРёС‡РµСЃРєРёР№ РїСЂРѕС†РµСЃСЃ РђРїРїР°СЂР°С‚Р° 511:**
   * **Р РµР°РєС‚РѕСЂ РђРїРїР°СЂР°С‚ 511 (R-511):** СЂРµР°РєС‚РѕСЂ СЃ РјРµС€Р°Р»РєРѕР№ Рё СЂСѓР±Р°С€РєРѕР№ РЅР°РіСЂРµРІР°; РєРѕРЅС‚СЂРѕР»СЊ С‚РµРјРїРµСЂР°С‚СѓСЂС‹ (РґРѕ 100 В°C), СЂРќ СЃСЂРµРґС‹ (9.5..12 РµРґ.), РІРµСЃР° (С‚РµРЅР·РѕРґР°С‚С‡РёРєРё WE-1, WE-2).
   * **Р›РёРЅРёСЏ Р’РѕРґС‹:** РѕС‚СЃРµС‡РЅРѕР№ РєР»Р°РїР°РЅ СЃ РєРѕРЅС†РµРІРёРєР°РјРё, СЂР°СЃС…РѕРґРѕРјРµСЂ Р­Р›Р•РњР•Р -Р Р­Рњ, РґРѕР·Р°С‚РѕСЂ Р·Р°РґР°РЅРЅРѕР№ РґРѕР·С‹ РІРѕРґС‹ (СѓСЃС‚Р°РІРєР° `rWaterDose`, РїСѓСЃРє `xWaterStart`).
   * **Р›РёРЅРёСЏ Р”РёР°Р·РѕСЃРѕРµРґРёРЅРµРЅРёСЏ:** СЂРµРіСѓР»РёСЂСѓСЋС‰РёР№ РєР»Р°РїР°РЅ СЃ РѕР±СЂР°С‚РЅРѕР№ СЃРІСЏР·СЊСЋ, СЂР°СЃС…РѕРґРѕРјРµСЂ, РџРР”-СЂРµРіСѓР»СЏС‚РѕСЂ СЂР°СЃС…РѕРґР°, Р°Р»РіРѕСЂРёС‚Рј РєРѕРЅС‚СЂРѕР»СЏ РѕРїСѓСЃС‚РѕС€РµРЅРёСЏ РµРјРєРѕСЃС‚Рё.
   * **Р›РёРЅРёСЏ Р•РґРєРѕРіРѕ РЅР°С‚СЂР° 14% (РЎРѕРґС‹):** СЂРµРіСѓР»РёСЂСѓСЋС‰РёР№ РєР»Р°РїР°РЅ СЃ РѕР±СЂР°С‚РЅРѕР№ СЃРІСЏР·СЊСЋ, СЂР°СЃС…РѕРґРѕРјРµСЂ, СЃС‚СѓРїРµРЅС‡Р°С‚С‹Р№ РёРЅС‚РµРіСЂР°С‚РѕСЂ СЂРќ-СЂРµРіСѓР»РёСЂРѕРІР°РЅРёСЏ.
   * **Р›РёРЅРёСЏ РџР°СЂР° 6 Р°С‚Рё:** СЂРµРіСѓР»РёСЂСѓСЋС‰РёР№ РєР»Р°РїР°РЅ РїР°СЂР° СЃ РѕР±СЂР°С‚РЅРѕР№ СЃРІСЏР·СЊСЋ, РџРР”-СЂРµРіСѓР»СЏС‚РѕСЂ РЅР°РіСЂРµРІР° СЃ СЂР°РјРїРѕР№ СЃРєРѕСЂРѕСЃС‚Рё Рё СЂРµР¶РёРјРѕРј СѓРґРµСЂР¶Р°РЅРёСЏ С‚РµРјРїРµСЂР°С‚СѓСЂС‹.
   * **РЎС‚Р°РґРёСЏ В«РЎРѕС‡РµС‚Р°РЅРёРµ / РљР°СЃРєР°РґРЅС‹Р№ СЃР»РёРІВ»:** РєРѕРЅРµС‡РЅС‹Р№ Р°РІС‚РѕРјР°С‚ СЃР»РёРІР° РґРёР°Р·Рѕ РїРѕРґ РєРѕРЅС‚СЂРѕР»РµРј СЂРќ СЃРѕРґС‹, РѕС‚РѕР±СЂР°Р¶РµРЅРёРµ РѕСЃС‚Р°РІС€РµРіРѕСЃСЏ РІСЂРµРјРµРЅРё Рё СЌС‚Р°РїР°.

### рџ“ђ Р Р°Р·СЂР°Р±РѕС‚Р°РЅРЅР°СЏ РєРѕРЅС„РёРіСѓСЂР°С†РёСЏ HMI
1. **РџРѕРґРєР»СЋС‡РµРЅРёРµ (`Connections`):**
   * `Id: "plc_511"`, `Type: "ModbusTCP"`, `Host: "127.0.0.1"`, `Port: 502`, `ByteOrder: "CDAB"`, `PollIntervalMs: 200`.
2. **РљР°СЂС‚Р° СЂРµРіРёСЃС‚СЂРѕРІ:**
   * Coils `00001`..`00013` (СЂРµР¶РёРјС‹ Р РЈР§/РђР’РўРћ, РїСѓСЃРєРё, РїР°СѓР·С‹, СЃС‚Р°С‚СѓСЃС‹).
   * Discrete Inputs `10500`..`10502` (СЃРѕР»РµРЅРѕРёРґ Рё РєРѕРЅС†РµРІРёРєРё РєР»Р°РїР°РЅР° РІРѕРґС‹).
   * Input Registers `30009`..`30045` (РѕР±СЂР°С‚РЅР°СЏ СЃРІСЏР·СЊ 3-С… РєР»Р°РїР°РЅРѕРІ, pH, С‚РµРјРїРµСЂР°С‚СѓСЂР°, РІРµСЃС‹, СЂР°СЃС…РѕРґС‹, РѕР±СЉРµРјС‹, РІСЂРµРјСЏ, РєРѕРґ С€Р°РіР°).
   * Holding Registers `40101`..`40157` (СѓСЃС‚Р°РІРєРё РєР»Р°РїР°РЅРѕРІ, С‚РµС…РЅРѕР»РѕРіРёС‡РµСЃРєРёРµ СѓСЃС‚Р°РІРєРё, РїР°СЂР°РјРµС‚СЂС‹ РџРР” Рё СЂРќ).
3. **P&ID РјРЅРµРјРѕСЃС…РµРјР° Mimic (10 px СЏС‡РµР№РєР°):**
   * Р¦РµРЅС‚СЂ: Р РµР°РєС‚РѕСЂ R-511 СЃ СЂСѓР±Р°С€РєРѕР№, С‚РµРјРїРµСЂР°С‚СѓСЂРѕР№ Рё РІРµСЃРѕРј.
   * Р’РµСЂС…: Р›РёРЅРёСЏ РІРѕРґС‹ (С‚СЂСѓР±Р° СЃ С„Р»Р°РЅС†Р°РјРё, СЃРѕР»РµРЅРѕРёРґРЅС‹Р№ РєР»Р°РїР°РЅ YV1, СЂР°СЃС…РѕРґРѕРјРµСЂ, Р·Р°РґР°С‚С‡РёРє РґРѕР·С‹, РєРЅРѕРїРєР° РїСѓСЃРєР° РґРѕР·С‹).
   * РЎР»РµРІР°-С†РµРЅС‚СЂ: Р›РёРЅРёСЏ РґРёР°Р·Рѕ (С‚СЂСѓР±Р°, СЂРµРіСѓР»РёСЂСѓСЋС‰РёР№ РєР»Р°РїР°РЅ FCV1, СЂР°СЃС…РѕРґРѕРјРµСЂ, СЃСѓРјРјР°С‚РѕСЂ, Р·Р°РґР°С‚С‡РёРє СЂР°СЃС…РѕРґР°).
   * РЎР»РµРІР°-РЅРёР·: Р›РёРЅРёСЏ СЃРѕРґС‹ (С‚СЂСѓР±Р°, СЂРµРіСѓР»РёСЂСѓСЋС‰РёР№ РєР»Р°РїР°РЅ FCV2, СЂР°СЃС…РѕРґРѕРјРµСЂ, СЃСѓРјРјР°С‚РѕСЂ).
   * РЎРЅРёР·Сѓ: Р›РёРЅРёСЏ РїР°СЂР° РІ СЂСѓР±Р°С€РєСѓ СЂРµР°РєС‚РѕСЂР° (С‚СЂСѓР±Р°, СЂРµРіСѓР»РёСЂСѓСЋС‰РёР№ РєР»Р°РїР°РЅ TCV1, СѓСЃС‚Р°РІРєР° Рў, РєРЅРѕРїРєРё РїСѓСЃРєР° Рё СѓРґРµСЂР¶Р°РЅРёСЏ).
   * РЎРїСЂР°РІР°: РџР°РЅРµР»СЊ РґР°С‚С‡РёРєРѕРІ Р°РїРїР°СЂР°С‚Р° (С‚РµРјРїРµСЂР°С‚СѓСЂР°, СЂРќ, РІРµСЃС‹) Рё РїР°РЅРµР»СЊ СЃС‚Р°РґРёРё СЃРѕС‡РµС‚Р°РЅРёСЏ (РїСѓСЃРє, РїР°СѓР·Р°, Р»Р°РјРїС‹ СЂР°Р±РѕС‚С‹/РіРѕС‚РѕРІРЅРѕСЃС‚Рё, С‚Р°Р№РјРµСЂ, СЌС‚Р°Рї).
4. **РћРїРµСЂР°С‚РѕСЂСЃРєРёР№ Dashboard (160 px СЏС‡РµР№РєР°):**
   * 4 С‚СЂРµРЅРґР° СЂРµР°Р»СЊРЅРѕРіРѕ РІСЂРµРјРµРЅРё: С‚РµРјРїРµСЂР°С‚СѓСЂР° R-511, СЂРќ СЃСЂРµРґС‹, СЂР°СЃС…РѕРґ РґРёР°Р·Рѕ, СЂР°СЃС…РѕРґ СЃРѕРґС‹.
   * РРЅР¶РµРЅРµСЂРЅС‹Рµ Р·Р°РґР°С‚С‡РёРєРё: СѓСЃС‚Р°РІРєР° Рў, СѓСЃС‚Р°РІРєР° РґРѕР·С‹, СѓСЃС‚Р°РІРєР° СЂР°СЃС…РѕРґР° РґРёР°Р·Рѕ, С†РµР»РµРІРѕР№ СЂРќ, РїР°СЂР°РјРµС‚СЂС‹ РџРР” РїР°СЂР°, РіСЂР°РЅРёС†С‹ СЂРќ.

### рџ§Є Р’РµСЂРёС„РёРєР°С†РёСЏ
* **РЎР±РѕСЂРєР° (`scripts/check-build.ps1`):** **0 РѕС€РёР±РѕРє, 0 РїСЂРµРґСѓРїСЂРµР¶РґРµРЅРёР№**.
* **Headless UI Runner (`scripts/render_ui.ps1 MimicView` & `DashboardView`):** РЎРіРµРЅРµСЂРёСЂРѕРІР°РЅС‹ СЌС‚Р°Р»РѕРЅРЅС‹Рµ СЃРЅРёРјРєРё, СЂР°Р·РјРµС‚РєР° РїСЂРѕРІРµСЂРµРЅР° РЅР° РѕС‚СЃСѓС‚СЃС‚РІРёРµ РЅР°Р»РѕР¶РµРЅРёР№ Рё РѕР±СЂРµР·РєРё С‚РµРєСЃС‚Р°.

---

## 2026-09-15 — Сессия 24: Копирование/вставка виджетов через контекстное меню и контейнеры, фиксация геометрии портов клапанов

### 🎯 Цель сессии
1. Реализовать копирование и вставку контролов через контекстное меню в режиме редактирования (Design Mode) с поддержкой вставки как на основную канву, так и внутрь виджета-контейнера (`ContainerButton`).
2. Устранить баг нарушения «магнитности» (нарушение стыковки и разрыв резиновых связей труб) при переключении типа клапана (`CutOff` <-> `Regulating`).

### 🔍 Анализ и локализация причин сбоя портов
* **Смещение центра клапана при наличии обратной связи:**
  В `VisualPortHelper.cs` и `ValveControl.cs` ранее высота рабочего поля уменьшалась:
  `availableH = isRegulating && showFeedbackBar ? h - 18 : h`.
  В результате при типе `Regulating` с источником обратной связи центр бабочки клапана $cy = availableH / 2$ смещался вверх на 9 пикселей ($9 / 20 = 0.45$ ячейки сетки).
  Стыковочные порты перескакивали с оси ячейки, из-за чего уже пристыкованные трубы переставали совпадать с координатами штуцеров ($|pt.Y - p1.Y| > 0.01$), резиновые связи при перемещении рвались, а новые трубы прилипали с визуальным смещением.

### 🛠️ Выполненные инженерные решения
1. **Фиксация геометрии портов и бабочки клапана (`ValveControl.cs`, `VisualPortHelper.cs`):**
   * В `ValveControl.cs` центр бабочки клапана жестко зафиксирован в геометрическом центре виджета: $center = (w/2, h/2)$, $flowSize = isFlowVertical ? h : w$, $cy = h/2$ независимо от `ValveType` и наличия шкалы.
   * Шкала обратной связи рендерится у нижнего края виджета ($barY = \max(cy + crossSize/2 + 2, h - barH - 2)$) без деформации или смещения тела клапана.
   * В `VisualPortHelper.GetVisualPortsInGrid` расчет портов синхронизирован с физическим центром $(w/2, h/2)$ и полным размером $flowSize$. В результате координаты портов CutOff и Regulating стали математически тождественными.
2. **Универсальный буфер обмена (`Services/WidgetClipboard.cs`):**
   * Создан статический сервис `WidgetClipboard` с методами `Copy(WidgetConfig)` и `PasteClone()`, реализующий глубокое клонирование через полиморфный `WidgetJsonConverter`.
3. **Команды копирования и вставки (`DashboardViewModel.cs`, `DashboardPanel.cs`):**
   * В `DashboardViewModel.cs` добавлены `CopyWidgetCommand`, `PasteWidgetCommand`, `PasteWidgetAt(col, row)` и `AddWidgetFromConfig(config)`.
   * В контекстном меню виджета добавлены пункты «Копировать» и «Вставить».
   * В `DashboardPanel.cs` добавлена обработка правого клика по пустому пространству канвы `ShowCanvasContextMenu(clickPoint)` со вставкой точно в ячейку клика.
   * В `DashboardPanel.OnKeyDown` добавлена поддержка клавиатурных сочетаний `Ctrl+C` и `Ctrl+V`.
4. **Поддержка контейнеров `ContainerButton`:**
   * В `ContainerButtonViewModel.cs` снята блокировка открытия в режиме редактирования.
   * Реализована команда `PasteWidgetIntoContainerCommand` / метод `PasteWidgetIntoContainer()`, вставляющий клонированный виджет в `Children` контейнера и динамически обновляющий открытое окно контейнера.
   * В контекстное меню контейнера добавлены пункты «Открыть контейнер» и «Вставить внутрь контейнера».
   * В `WidgetEditorViewModel.cs` обеспечено сохранение дочерней коллекции `Children` при редактировании параметров контейнера.

### 🧪 Верификация и результаты
* **5 автоматических валидационных проверок в `AvaloniaApplication1.UIValidation/Program.cs`:**
  1. `WidgetClipboard` для простых виджетов: УСПЕШНО.
  2. `WidgetClipboard` для контейнеров с иерархией детей: УСПЕШНО.
  3. `ContainerButtonViewModel.PasteWidgetIntoContainer`: УСПЕШНО.
  4. Тождественность координат портов CutOff и Regulating ($\Delta < 0.001$ px): УСПЕШНО.
  5. `DashboardViewModel.PasteWidgetAt`: УСПЕШНО.
* **Сборка (`scripts/check-build.ps1`):** **0 ошибок, 0 предупреждений**.
* **Рендеринг (`scripts/render_ui.ps1 MimicView` & `DashboardView`):** Успешно, скриншот `artifacts/ui_preview.png` подтверждает безупречную соосность клапанов YV1, FCV1, FCV2, TCV1 и труб.

## 2026-09-15 — Сессия 25: Контекстное меню рабочей области, добавление элементов и диалог свойств сетки/масштаба

### 🎯 Цель сессии
1. Обеспечить надежный вызов контекстного меню в режиме редактирования (Design Mode) в любой точке рабочей области (холста и пустых зон `DashboardView`).
2. Добавить в контекстное меню канвы пункт «Добавить элемент...» с вызовом диалога настройки по координатам клика, а также подменю быстрого добавления любых типов технологических аппаратов в 1 клик.
3. Добавить пункт «Свойства...» с диалогом настройки размера сетки (Cell Size) и масштаба (Zoom Scale).
4. Реализовать полноценную работу контекстного меню, добавления элементов, вставки и настройки параметров сетки/масштаба внутри дочерних окон контейнеров (`ContainerButton`).

### 🛠️ Выполненные инженерные решения
1. **Устранение мертвых зон вызова контекстного меню (`DashboardView.axaml.cs`, `DashboardPanel.cs`):**
   * В `DashboardView.axaml.cs` добавлен обработчик `OnBubblePointerPressed` с фазой `RoutingStrategies.Bubble`, перехватывающий клики ПКМ по фоновым областям, полям и отступам `ScrollViewer` вне фактических границ виджетов и транслирующий их в `DashboardPanel.ShowCanvasContextMenu(posInPanel)`.
   * В `DashboardPanel.cs` метод `ShowCanvasContextMenu(Point clickPoint)` объявлен `public`.
   * В `DashboardPanel.MeasureOverride` минимальный размер панели в режиме редактирования увеличен до 50x40 ячеек, гарантируя непрерывное покрытие сетки.
2. **Добавление элементов через контекстное меню канвы (`DashboardPanel.cs`, `DashboardViewModel.cs`):**
   * Пункт «➕  Добавить элемент...»: открывает `ShowWidgetEditorAsync` с автоматической передачей координат ячейки `(col, row)`.
   * Подменю «⚡  Быстро добавить»: позволяет в 1 клик разместить на сетке любой аппарат: Задвижка/Клапан, Насос, Трубопровод, Бак, Теплообменник, Реактор с мешалкой, Датчик уровня, Контейнер, Значение, Уставка, Тренд, Кнопка, Лампа, Ползунок.
   * Реализованы методы `AddWidgetAtAsync(double col, double row)` и `AddQuickWidgetAt(string type, double col, double row)` в `DashboardViewModel.cs`.
3. **Диалог свойств рабочей области («Свойства...»):**
   * Созданы окно `Views/DashboardPropertiesWindow.axaml` и вьюмодель `ViewModels/DashboardPropertiesViewModel.cs`.
   * Настройка шага сетки: быстрые пресеты `10 px`, `20 px`, `40 px`, `80 px`, `160 px` и поле точной подстройки `NumericUpDown` (от 10 до 500 px).
   * Настройка масштаба: пресеты `50%`, `75%`, `100%`, `125%`, `150%`, `200%`, плавный слайдер и `NumericUpDown` (от 20% до 300%).
   * Кнопки «🔄 По умолчанию» (40 px / 100%), «Отмена» и «Применить».
   * Свойства `CellWidth`, `CellHeight` и `ZoomScale` в `DashboardViewModel` переведены на реактивные `[ObservableProperty]`.
   * В `DashboardPanel` добавлены обработчики `CellWidthProperty.Changed` и `CellHeightProperty.Changed` для мгновенной перерисовки `GridOverlay` при смене шага сетки.
4. **Синхронизация с окнами-контейнерами `ContainerButton`:**
   * В `ContainerButtonConfig` добавлены поля `CellSize` и `ZoomScale`.
   * В `ContainerButtonViewModel.OpenContainerAsync` параметры сетки и масштаба передаются в дочернее окно и синхронизируются через событие `innerDashboardVm.OnGridOrScaleChanged`.
   * В редактор виджетов `WidgetEditorWindow.axaml` добавлена панель `Container Window Settings` (`ContainerCellSize`, `ContainerZoomPercent`).

### 🧪 Верификация и результаты
* **8 автоматических валидационных проверок в `AvaloniaApplication1.UIValidation/Program.cs`:**
  1. `WidgetClipboard` для простых виджетов: УСПЕШНО.
  2. `WidgetClipboard` для контейнеров: УСПЕШНО.
  3. `ContainerButtonViewModel.PasteWidgetIntoContainer`: УСПЕШНО.
  4. Тождественность координат портов CutOff и Regulating: УСПЕШНО.
  5. `DashboardViewModel.PasteWidgetAt`: УСПЕШНО.
  6. `DashboardPropertiesViewModel` (пресеты, масштаб, сброс): УСПЕШНО.
  7. `DashboardViewModel.UpdateGridAndScale` и `AddQuickWidgetAt`: УСПЕШНО.
  8. Синхронизация `CellSize` и `ZoomScale` между окном `ContainerButton` и конфигурацией: УСПЕШНО.
* **Сборка (`scripts/check-build.ps1`):** **0 ошибок, 0 предупреждений**.
* **Headless UI Рендеринг (`scripts/render_ui.ps1 MimicView` и `DashboardView`):** Все артефакты `ui_preview.png` и `ui_tree.json` успешно сформированы.

## 2026-09-15 — Сессия 26: Устранение «фантомного» удаления виджетов и декаплинг оверлеев

### 🎯 Цель сессии
Устранить дефект, при котором после удаления виджета через контекстное меню («Удалить») или по клавише `Delete` элемент иногда оставался визуально отображаться на экране (фантом), исчезая только после переключения активных вкладок «Дашборд» и «Мнемосхема». Обеспечить мгновенное, детерминированное удаление визуального контейнера виджета с холста.

### 🔍 Анализ коренной причины (Root Cause Analysis)
1. **Засорение `ItemsControl.ItemsPanel.Children`:**
   В Avalonia `DashboardPanel` является панелью элементов (`ItemsPanel`) для `ItemsControl` со связкой `ItemsSource="{Binding Widgets}"`. Avalonia связывает индекс элемента в коллекции `ItemsSource` (`Widgets[i]`) с индексом визуального контейнера `Children[i]`.
   Однако в старой реализации `GridOverlay` вставлялся как `Children.Insert(0, _gridOverlay)`, а `SelectionOverlay` как `Children.Add(_selectionOverlay)`.
   Когда пользователь удалял виджет с индексом `0` (`Widgets.RemoveAt(0)`), `ItemsControl` удалял из `Children` элемент с индексом `0` — то есть **сам служебный `_gridOverlay`**! При этом реальный контейнер удаленного виджета оставался на холсте как «фантом». Метод `EnsureOverlaysState()` при последующем цикле вставлял новый `_gridOverlay` в начало, создавая постоянное рассогласование индексов.
2. **Преждевременный сброс выделения (`SelectedVm = null`):**
   В обработчике контекстного меню свойство `SelectedVm` сбрасывалось до вызова `RemoveWidgetCommand.Execute(...)`. Из-за этого свойство `widget.IsSelected` становилось `false`, и фоллбэк команды удаления `widget ??= Widgets.FirstOrDefault(w => w.IsSelected)` возвращал `null`, не выполняя удаление.

### 🛠️ Выполненные инженерные решения
1. **Архитектурный вынос оверлеев из `DashboardPanel.Children` (`DashboardView.axaml`):**
   * Внутри `ZoomControl` развернут трехуровневый `<Grid>`:
     - **Слой 1 (фон):** `<v:GridOverlay x:Name="DashboardGridOverlay" Margin="10" IsHitTestVisible="False" />`
     - **Слой 2 (контент):** `<ItemsControl ItemsSource="{Binding Widgets}" x:Name="WidgetsItemsControl"> ... </ItemsControl>`
     - **Слой 3 (передний план):** `<v:SelectionOverlay x:Name="DashboardSelectionOverlay" Margin="10" IsHitTestVisible="False" ClipToBounds="False" />`
   * Панель `DashboardPanel` теперь содержит **исключительно** визуальные контейнеры виджетов. Никаких вставок оверлеев в `Children`, никаких вызовов `EnsureOverlaysState()`.
2. **Декаплинг `GridOverlay.cs` и `SelectionOverlay.cs`:**
   * Добавлены конструкторы по умолчанию `public GridOverlay()` и `public SelectionOverlay()`.
   * Реализован автоматический резолв целевой `DashboardPanel` через визуальное дерево при `OnAttachedToVisualTree` и метод явного связывания `panel.LinkOverlays(gridOverlay, selectionOverlay)`.
   * В `DashboardView.axaml.cs` в `OnApplyTemplate` и `InitializeComponent` выполняется надежное связывание через `LinkOverlays`.
3. **Коррекция команды удаления в `DashboardPanel.cs`:**
   * В `ShowWidgetContextMenu`: удаляемый экземпляр виджета кэшируется в локальной переменной `var toRemove = hitWidget`, после чего вызывается `dashboardVm.RemoveWidgetCommand.Execute(toRemove)`, и только затем очищается `SelectedVm = null; InvalidateVisual();`.
   * Аналогично скорректирован обработчик `OnKeyDown` для клавиши `Delete`.
4. **Тест №9 в `AvaloniaApplication1.UIValidation/Program.cs`:**
   * Добавлен автоматический тест: добавление 3 виджетов в `DashboardViewModel.Widgets` и панель `DashboardPanel`, затем поочередное удаление (среднего, первого, последнего) с верификацией строгого равенства `DashboardPanel.Children.Count` ожидаемым значениям (2, 1, 0) без переключения вкладок.

### 🧪 Верификация и результаты
* **9 автоматических валидационных проверок в `AvaloniaApplication1.UIValidation/Program.cs`:** Все 9 тестов пройдены успешно (100%).
* **Сборка (`scripts/check-build.ps1`):** **0 ошибок, 0 предупреждений**.
* **Headless UI Рендеринг (`scripts/render_ui.ps1 DashboardView`):** Рендеринг холста и оверлеев прошел штатно, артефакты `artifacts/ui_preview.png` и `artifacts/ui_tree.json` успешно созданы.

## 2026-09-15 — Сессия 27: Динамическое следование рамки выделения трубы и реактивная синхронизация контейнера

### 🎯 Цель сессии
Устранить дефект, при котором после модификации или добавления вершин трубы она визуально смещалась относительно синей рамки выделения (рамка оставалась внизу в виде плоского прямоугольника, а труба уходила вверх). При переключении вкладок «Дашборд» <-> «Мнемосхема» труба возвращалась на правильное место. Обеспечить непрерывное динамическое следование рамки выделения за вершинами трубы в реальном времени и мгновенную синхронизацию контейнера виджета без необходимости смены экранов.

### 🔍 Анализ коренной причины (Root Cause Analysis)
1. **Приоритет свойств в Avalonia (`BindingPriority.LocalValue` vs `BindingPriority.Style`):**
   При обычном перемещении виджета метод `DashboardPanel.OnPointerReleased` устанавливал `SetCol(_dragChild, newCol)` и `SetRow(_dragChild, newRow)` напрямую на визуальный контейнер (`ContentPresenter`). В Avalonia прямое присвоение создает `LocalValue`, которое имеет наивысший приоритет и блокирует обновление координат из стиля контейнера (`DashboardView.axaml`).
2. **Отсутствие реакции панели на пересчет координат ViewModel:**
   Когда пользователь добавлял, удалял или перемещал вершину трубы, метод `PipeWidgetViewModel.NormalizePointsAndSize()` пересчитывал `newCol`, `newRow`, `newSizeX`, `newSizeY` и обновлял относительные координаты `PipePoints`. Однако панель `DashboardPanel` **не была подписана на `vm.PropertyChanged`**. В результате контейнер `ContentPresenter` оставался на устаревших координатах `(oldCol, oldRow)`.
3. **Физический сдвиг трубы внутри устаревшего контейнера:**
   Контрол `PipeControl` перерисовывал новые относительные координаты `newPipePoints` внутри старого контейнера, физически сдвигая трубу на холсте на величину разницы `(newCol - oldCol, newRow - oldRow)`.
4. **Застревание `SelectionOverlay`:**
   Оверлей выделения вычислял позицию рамки через `GetCol(selectedControl)`, получая устаревшее локальное значение `oldCol, oldRow`.
5. **Эффект переключения вкладок:**
   При смене вкладки `ItemsControl` пересоздавал контейнеры `ContentPresenter` с нуля, читая актуальные `vm.Col` и `vm.Row`, из-за чего труба и рамка внезапно «прыгали» на истинную позицию.

### 🛠️ Выполненные инженерные решения
1. **Реактивная синхронизация в `DashboardPanel.cs`:**
   * Добавлены методы жизненного цикла дочерних элементов: `SubscribeChildVm(Control child)` и `UnsubscribeChildVm(Control child)`, вызываемые при добавлении/удалении контролов из `Children`.
   * Добавлен централизованный обработчик `OnWidgetVmPropertyChanged`: при изменении `Col`, `Row`, `SizeX`, `SizeY` или `PipePoints` у любой `WidgetViewModelBase` немедленно обновляются присоединенные свойства контейнера:
     ```csharp
     SetCol(child, vm.Col);
     SetRow(child, vm.Row);
     SetSizeX(child, vm.SizeX);
     SetSizeY(child, vm.SizeY);
     ```
     и вызываются `InvalidateMeasure()`, `InvalidateArrange()`, `InvalidateVisual()` и `_selectionOverlay?.InvalidateVisual()`.
   * Реализован публичный метод `FindChildForVm(WidgetViewModelBase vm)` для надежного поиска контейнера в коллекции `Children`.
   * В `DashboardPanel.OnPointerMoved` при интерактивном перетаскивании вершины трубы добавлен вызов `_selectionOverlay?.InvalidateVisual()` на каждый тик мыши.
   * В `DashboardPanel.OnPointerReleased` при отпускании вершины трубы добавлен вызов синхронизации `FindChildForVm(pipeVm)` и инвалидации лейаута.
   * В `DashboardPanel.OnPointerReleased` для rubber-banding подключенных труб при перетаскивании задвижек/насосов добавлена синхронизация контейнеров труб.
   * В `DashboardPanel.OnPreviewPointerPressed` заблокирован ложный захват ресайза правого нижнего угла для трубы в режиме `IsEditingVertices`.
2. **Динамический расчет рамки выделения в `SelectionOverlay.cs`:**
   * Для `PipeWidgetViewModel` рамка вычисляется напрямую из реальных абсолютных координат вершин `pipeVm.GetAbsoluteGridPoints()`:
     ```csharp
     double minX = absPoints.Min(p => p.X);
     double maxX = absPoints.Max(p => p.X);
     double minY = absPoints.Min(p => p.Y);
     double maxY = absPoints.Max(p => p.Y);
     col = Math.Floor(minX);
     row = Math.Floor(minY);
     sizeX = Math.Max(1.0, Math.Ceiling(maxX - col));
     sizeY = Math.Max(1.0, Math.Ceiling(maxY - row));
     ```
     Это строго соответствует формуле `NormalizePointsAndSize` и обеспечивает безупречное следование рамки за всеми сегментами трубы и во время драга, и после нормализации.
3. **Оповещение панели в `PipeControl.cs`:**
   * Добавлен вспомогательный метод `NotifyPanelAfterPointsChanged()`, вызываемый при контекстных действиях: `AddPointOnSegment`, `AddPointAtStart`, `AddPointAtEnd`, `RemovePointAt`.
4. **Тест №10 в `AvaloniaApplication1.UIValidation/Program.cs`:**
   * Добавлен автоматический тест:
     - Создание `PipeWidgetViewModel` на позиции (10, 10) размером (10, 1).
     - Добавление вершины с отрицательным смещением `(-3, -2)`.
     - Автоматическая нормализация в VM: `Col` переходит в 7, `Row` в 8.
     - Проверка мгновенной синхронизации `DashboardPanel.GetCol(pipeChild) == 7` и `GetRow(pipeChild) == 8` без смены экранов.
     - Проверка инвариантности абсолютных координат остальных точек трубы.
   * Зарегистрирован `IChildWindowService` в DI тестового раннера.

### 🧪 Верификация и результаты
* **10 автоматических валидационных проверок в `AvaloniaApplication1.UIValidation/Program.cs`:** Все 10 тестов успешно пройдены (100% PASS).
* **Сборка (`scripts/check-build.ps1`):** **0 ошибок, 0 предупреждений**.
* **Headless UI Рендеринг (`scripts/render_ui.ps1 MimicView` и `DashboardView`):**
  - `artifacts/ui_preview.png` и `artifacts/ui_tree.json` успешно сгенерированы.
  - Проведен мультимодальный анализ скриншотов: отсутствие клиппинга, наложений, идеальная стыковка труб и клапанов.

---

## 📅 15.09.2026 (Сессия 28 — Контролы выбора цвета и формата: ColorPickerBox + FormatEditorBox, режимы кнопок Latching/ToggleSwitch)

### 📌 Достижение: Внедрение переиспользуемых UserControl для редактирования цвета и формата, расширение типов CommandButton
*   **ColorPickerBox (`Views/Controls/ColorPickerBox.axaml` + `.cs`):**
    *   36 цветовых ячеек (6x6 UniformGrid) с промышленной палитрой HMI/SCADA.
    *   RGB-слайдеры с `NumericUpDown` для покомпонентного ввода каналов (0–255).
    *   Спектральный слайдер оттенка (Hue 0–360°) с радужным градиентом.
    *   `TextBox` прямого ввода Hex-значений с валидацией и двухсторонней синхронизацией.
    *   Архитектура: `DataContext = this`, `x:CompileBindings="False"`, `INotifyPropertyChanged`, StyledProperty `SelectedColorHex` (TwoWay).
*   **FormatEditorBox (`Views/Controls/FormatEditorBox.axaml` + `.cs`):**
    *   `MenuFlyout` с 11 технологическими категориями (~50 пресетов): Температура, Давление, Расход, Уровень, Масса, Химия, Электрика, Обороты, Объем, Время, Числа.
    *   5 кнопок быстрой точности (0 / .0 / .00 / .000 / Авто) с сохранением суффикса единицы измерения.
    *   StyledProperty `Format` (TwoWay).
*   **CommandButton (режимы Latching и ToggleSwitch):**
    *   Добавлены режимы с фиксацией и тумблером: `LabelOn`, `LabelOff`, `ColorOn`, `ColorOff`.
*   **Интеграция в `WidgetEditorWindow.axaml`:**
    *   Редактирование формата и цвета значений (`FormatEditorBox`, `ColorPickerBox`).
    *   Выбор цветов состояний ламп и аппаратов (`TrueColor`, `FalseColor`, `ActiveColor`, `InactiveColor`).
*   **Сборка и тесты:** 0 ошибок, 0 предупреждений, 10 тестов UIValidation успешно пройдены.

---

## 📅 21.09.2026 (Сессия 29 — Механизм проектов: Загрузка выбранного конфига и «Сохранить как...»)

### 📌 Достижение: Полноценное управление проектами/конфигурациями HMI в Design Mode
*   **Сервис конфигурации (`IConfigurationService`, `ConfigurationService`):**
    *   Добавлено свойство `CurrentFilePath` для отслеживания пути к текущему активному файлу проекта (по умолчанию `config.json`).
    *   Методы `LoadConfigurationAsync(string? filePath = null)` и `SaveConfigurationAsync(HmiConfiguration config, string? filePath = null)` расширены поддержкой произвольных путей с автосозданием родительских папок и обновлением `CurrentFilePath`.
*   **Сервис диалогов (`IDialogService`, `DialogService`):**
    *   Интегрирован кроссплатформенный `StorageProvider` из Avalonia (`TopLevel.GetTopLevel(MainWindow)?.StorageProvider`).
    *   Реализованы методы `ShowOpenProjectDialogAsync()` и `ShowSaveProjectAsDialogAsync(string? defaultFileName = null)` с фильтрами `*.json` («Файлы проекта HMI») и `*.*` («Все файлы»).
    *   Устранены предупреждения компилятора CS8602 в `DialogService.cs`.
*   **Главная ViewModel (`MainViewModel`):**
    *   Внедрены свойства `CurrentProjectName` и `WindowTitle` с динамическим отображением имени открытого проекта в заголовке окна: `Avalonia HMI Dashboard - [<Имя проекта>]`.
    *   Внедрена команда `OpenProjectCommand`: вызывает системный диалог выбора файла, корректно останавливает драйверы связи (`_dataCoreService.StopAsync()`), утилизирует старые дашборды и MDI-окна, перезагружает конфигурацию, пересоздает дашборды и запускает драйверы связи для новых тегов с выводом Toast-уведомления.
    *   Внедрена команда `SaveConfigAsCommand`: открывает системный диалог «Сохранить как...», сохраняет конфигурацию по новому пути, обновляет имя проекта и выводит Toast.
    *   Команда `SaveConfigCommand` обновлена выводом информативного Toast-уведомления.
    *   Устранено предупреждение компилятора CS8604 в `OpenChildWindow`.
*   **Пользовательский интерфейс (`MainWindow.axaml`):**
    *   В панель инструментов режима редактирования (Design Mode) добавлены:
        *   Стилизованный бейдж с именем текущего файла проекта: `📁 <ИмяПроекта>`.
        *   Кнопка `📂 Загрузить...` (`OpenProjectCommand`).
        *   Кнопка `💾 Сохранить` (`SaveConfigCommand`).
        *   Кнопка `💾 Сохранить как...` (`SaveConfigAsCommand`).
    *   Добавлены глобальные горячие клавиши: `Ctrl+O` (Open Project), `Ctrl+S` (Save Project), `Ctrl+Shift+S` (Save Project As).
    *   Свойство `Title` окна привязано к `WindowTitle`.
*   **Конфигурация проекта (`AvaloniaApplication1.csproj`):**
    *   Добавлена настройка `<UseAppHost>false</UseAppHost>`, предотвращающая сбои блокировки Win32 PE-файлов драйвером Google Drive при инкрементальной компиляции.
*   **Сборка и верификация:**
    *   Проект собирается со статусом **0 ошибок, 0 предупреждений C# компилятора**.

---

## 📅 21.09.2026 (Сессия 30 — Создание HMI-конфигурации парка хранения ММА и руководство по компиляции/развертыванию на панель RealLab Debian 9)

### 📌 Достижение: Полнофункциональная HMI SCADA конфигурация для парка 4 емкостей ММА 500 м³ и сборочный пайплайн под панельный контроллер RealLab (BCM2837B0)

*   **Анализ проектной документации и ПЛК Wiren Board 8:**
    *   Изучены чертежи и схемы из `C:\Users\adm\Desktop\Парк хранения ММА4 емкости`: P&ID `Проект.pdf`, `Проект. Этап 2.pdf`, спецификации оборудования `СП-21-Монтаж 4-х емкостей ММА 500 м3` (лист 10 — панель NLcon-LXD12-IP65 компании RealLab ООО «НИЛ АП»).
    *   Проанализированы C++ исходники прошивки контроллера (`PLC/cex21_park_mma/src/Mem/`): `IR.hpp` (30001–30065), `HR.hpp` (40001–40029), `CL.hpp` (00001–00019), `IN.hpp` (10001–10031).
    *   Выполнено 100% сопоставление Modbus TCP регистров для всех контуров: емкости 22, 23, 24, 25; насосы перекачки 502в, 502г, 502д; отсечные клапаны приема LV 22..25; клапаны слива V 22..25; расходомеры FT 1-1, FT 2-1; дозирование налива в автоцистерны; азотная подушка; система ПАЗ.

*   **Разработка HMI-конфигурации (`config_mma_park.json`):**
    *   Создано подключение `wb8_plc` (Modbus TCP `10.10.10.234:502`, порядок байт `CDAB`, unit id 1, период опроса 200 мс) и MQTT брокер `wb8_mqtt` (`10.10.10.234:1883`).
    *   **Мнемосхема Mimic (1024x768):**
        *   4 резервуара ММА по 500 м³ (поз. 22–25) с индикацией уровня (30001, 30003, 30005, 30007), аварийными уровнями Hi/Lo и цветами состояний.
        *   3 технологических насоса (502в, 502г, 502д) с командами Пуск/Стоп (Coils 00006–00008) и статусами работы (Discrete Inputs 10006–10008).
        *   Входные отсечные клапаны LV 22..25 (Coils 00001–00004) и сливные краны V 22..25.
        *   Расходомеры FT 1-1 и FT 2-1 (мгновенный и суммарный расход) и контур дозирования (Holding Regs 40001–40002, Coils 00013–00014).
        *   Трубопроводные линии приема, перекачки и слива с фланцевыми соединениями.
    *   **Операторский Dashboard:**
        *   Шкалы уровней резервуаров с процентной шкалой и объемами.
        *   Тренды мгновенных расходов FT 1-1 и FT 2-1 в реальном времени.
        *   Панель управления дозированием автоналива (уставка дозы, запуск, стоп, сброс счетчика).
        *   Панель контроля азотной подушки и давления дыхательных клапанов.
        *   Светосигнальная панель аварийных сигнализаторов ПАЗ (загазованность, перелив, пожар).
    *   Конфиг размещен в проекте `AvaloniaApplication1/config_mma_park.json` и скопирован в папку заказчика `C:\Users\adm\Desktop\Парк хранения ММА4 емкости\config_mma_park.json`.

*   **Руководство и сборочный пайплайн для панели RealLab (`docs/reallab_panel_guide.md`):**
    *   Анализ аппаратной платформы NLcon-LXD12-IP65: SoC Broadcom BCM2837B0 (4 ядра Cortex-A53 @ 1.2 ГГц, 1 ГБ RAM, экран 12.1" 1024x768).
    *   ОС: RealLab! Raspbian Linux на базе Debian 9 Stretch. Ограничение: версия libc — `glibc 2.24`.
    *   Технический анализ проблемы: официальный рантайм .NET 8 CoreCLR требует `glibc >= 2.27`.
    *   Разработаны три детальных инженерных сценария:
        1.  *Рекомендуемый (Production):* Обновление дистрибутива на панели до Debian 11 Bullseye / Debian 12 Bookworm (ядро BCM2837B0 нативно поддерживает armhf и arm64 с glibc 2.31/2.36), сборка self-contained .NET 8 (`linux-arm64` или `linux-arm`).
        2.  *Сборка без модификации прошивки панели:* Мультитаргетинг в .NET 6.0 LTS (полная совместимость с glibc 2.23+ в Debian 9 Stretch).
        3.  *Запуск через изолированный sysroot glibc 2.28+* с помощью patchelf и запуска через `ld-linux`.
    *   Подготовлены скрипты компиляции и сборки: `scripts/build_reallab_panel.ps1` (PowerShell) и `scripts/build_reallab_panel.sh` (Linux).
    *   Описаны запуск через Linux Framebuffer (DRM/KMS без X11) и systemd unit файл для автозапуска.

*   **Автоматизированное тестирование и верификация:**
    *   В `AvaloniaApplication1.UIValidation/Program.cs` добавлен Тест №11:
        *   Валидация десериализации `config_mma_park.json` в модель `HmiConfiguration`.
        *   Проверка корректности параметров подключения Modbus TCP (10.10.10.234:502, `CDAB`, unit 1).
        *   Проверка наличия вкладок дашборда и мнемосхемы, а также всех типов виджетов (Tank, Pump, Valve, Pipe, ValueDisplay, SetValue, RealTimeTrend, PilotLight, CommandButton).
        *   Проверка успешной инициализации `DashboardViewModel` и инстанцирования виджетов без исключений.
    *   Все 11 автоматических тестов успешно пройдены (100% PASS).
    *   Сборка решения `AvaloniaApplication1.csproj`: **0 ошибок, 0 предупреждений**.